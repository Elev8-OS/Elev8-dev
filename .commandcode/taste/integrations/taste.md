# integrations
- Don't mention third-party integration provider brand names (e.g., Seam) anywhere in the UI except within the integration settings/configuration page itself. Confidence: 0.75
- When a mock/demo action exists in an integration UI (e.g. "Simulate finalize"), model it on the real-world event it stands for — the provider's webhook and its state transition (e.g. Lexware `invoice.changed`: `draft_created` → `open_in_lexware`) — not on a generic "test connection" action. Users will question the semantics until they match the real flow. Confidence: 0.6
- Integration-driven options in other modules — both TRIGGERS (e.g. an "Email Received" trigger in Journeys) AND message-channel choices (e.g. the WhatsApp channel in a journey "Send a Message" step and its If/Else branch channel picker) — must be gated by the integration's connection state: the option is disabled and shows a "Not connected" status pill (gray dot + "Not connected" vs green dot + "Connected") until the integration is connected, mirroring the Minut/Email trigger gate. The user reported "bug whatsapp not connected yet but can choose whatsapp on journey", confirming gate-by-connected generalizes from triggers to channel options too, and it must be applied in BOTH the main config and the branch dialog. Confidence: 0.7
- A connection-gated option should NOT just be greyed out — also surface an inline "Connect {X}" link/button that navigates to the integration's settings page (e.g. `/settings/integrations`) so there's a one-click path to connect. The user asked "if not connected on journey, should be theres a button or link to integration whatsapp page", and the accepted form is a muted helper line below the selector ("WhatsApp isn't connected.") with a primary `NuxtLink` "Connect WhatsApp", applied in BOTH the message step and the If/Else branch channel picker. Confidence: 0.7
- When a connection-gated option is not connected, the option itself should stay CLICKABLE rather than disabled/greyed-out, but clicking it should NOT navigate away — it should only REVEAL an inline "Connect {X}" hint below the selector, and only clicking that hint's link navigates to the integration's settings page (e.g. `/settings/integrations`). The user's follow-up "ini WhatsApp isn't connected. Connect WhatsApp hanya muncul pas user klik option whatsapp, bukan langsung di optionnya diklik pindah" walked back the earlier navigate-on-click behavior into a click-to-reveal: the unconnected option becomes a one-click path to the hint (not the connection page), the hint is gated by a shared reveal flag (`showWhatsAppConnectHint && !whatsappConnected`), and only when connected does clicking it actually switch the channel (locking to Template mode). Applied in BOTH the message-step and If/Else branch channel pickers. Confidence: 0.8
- Third-party integration connect flows should use OAuth ("buat flow login by oauth, jadi hapus flow masukan api key") instead of manual API-key entry forms — the user explicitly directed replacing the Minut API-key + workspace-name input flow with an OAuth "Connect with Minut" flow, consistent with the existing WhatsApp and 3CX OAuth patterns. Confidence: 0.75
- After connecting a device/IoT integration (e.g. Minut), the devices must be mapped to the user's own listings/properties — per-device listing dropdown with an explicit "Unassigned" option plus search/filter controls, and events from unmapped devices are excluded from listing-scoped features (e.g. Journey triggers) until mapped ("setelah connect harus map devicenya ke listing/property yang user punya"). Confidence: 0.7
- When the real target system has no public API (e.g. Indonesia's APOA web portal, AVS XML import), the user chose mock direct API submission for V1 over the recommended "generate file + manual import" — mock only the network/API layer while keeping real payloads (native field names per system) and state transitions ("mock tapi flow asli"), and defer real file/XML export to V2. Confidence: 0.6
- For multiple external systems sharing one reporting obligation (APOA Indonesia + AVS Meldeschein Germany), the user approved ONE shared module with a common pipeline and per-provider adapters (own payload builder with native field names, connection details, and provider-scoped reporting rules — APOA reports foreign nationals only, AVS reports every guest) rather than two parallel integrations. Confidence: 0.5
- Multiple connected accounts per provider must be supported for integration connections (e.g. several APOA and several AVS accounts, each with its own connection details): connecting ADDS an account rather than replacing the previous one, with an explicit "Add account" affordance and per-account tabs in the settings panel. Listings are then assigned to a specific account — each listing belongs to exactly one account per provider, reports/records route to the owning account, and disconnecting an account unassigns only its own listings while preserving submitted history. The user stated this capability explicitly: "i can also connect multiple avs and apoa account and assign to listing". Confidence: 0.7
- When the user redefines a provider's connection fields (e.g. AVS from `firmName`/`username`/`propertyNumber` to Account Name / Kurverwaltung ID / Firma ID / Objekt ID / Objekt ID (Airbnb)), propagate the change through the WHOLE adapter surface in one pass, not just the form: the connection-details type, the payload builder (with native per-system field names, e.g. `kurverwaltung_id`/`firma_id`/`objekt_id`), the payload-owning record model (add a `channel` field when a value must differ by booking source, e.g. Airbnb vs direct), the composable that fills the record from its source entity, the connect form + reset/validation/submit, the account label/detail display, and any cross-module consumers (e.g. the listing picker label). Channel-sourced fields fall back to the direct/default value when the channel-specific value is absent. Confidence: 0.7
- Organize the integrations page into themed functional groups (each with an icon + section heading), not a flat list of sibling cards. The user first asked to group same-obligation integrations (APOA, AVS, Feratel under "Government Integration"), then explicitly followed up "dan buat group untuk integrasi yang lain juga" ("and make groups for the other integrations too"), so grouping generalizes to the WHOLE page: Messaging & Communication (WhatsApp, Email, 3CX Telephony), Devices & Sensors (Smart Lock, Minut), Payments (Payout Gateways), and Government Integration (APOA, AVS Meldeschein, Feratel). Cards inside each group keep their status pill + Connect/Manage button. Confidence: 0.7
- When an integration's underlying vendor (e.g. Minut) is already integrated inside the project's PMS (HostBuddy), route Elev8 through the HostBuddy API as the bridge rather than integrating with the vendor directly — Elev8 never talks to the Minut API directly; device reads and event delivery go through HostBuddy. Confidence: 0.6
- For real-time event ingestion from a sensor/IoT integration (e.g. Minut noise/smoke/temperature/motion events), prefer webhook (push) delivery over polling — Elev8 exposes a signed webhook endpoint that receives events from the provider/bridge (HostBuddy) and persists/dispatches them, rather than the client polling for new events. Confidence: 0.55
- When a connection-gated channel is selected while its integration is still disconnected, the channel should actually SWITCH to it AND the entire dependent editing surface must be replaced by a single "not connected" prompt (amber banner with a Connect button linking to the integration settings) — NOT merely an inline hint below the selector that leaves the previous channel's editor visible. The user's complaint ("harusnya kalau whatsapp not connected ini AI Directive/Template ... ngga muncul") superseded the earlier click-to-reveal-hint approach, which never switched the channel and so kept the OTA message editor (AI Directive/Template toggle, static text, variable chips, AI Context Check, AI Personalization) on screen. Selecting an unconnected channel swaps the editor for the connect gate; only once connected does the normal (template-only, for WhatsApp) editor render. Applied to both the journey message step and the If/Else branch message config. Confidence: 0.75
- When an integration disconnect silently breaks dependent entities (e.g. active journeys whose message step uses the WhatsApp channel), proactively AUTO-DEACTIVATE those entities on the disconnect transition rather than leaving them "active" while their dependent steps would fail/skip: flip status active→inactive for every matching entity, surface a toast naming the count ("N journeys paused because WhatsApp disconnected."), and keep a persistent warning indicator next to the entity's status (amber triangle icon + tooltip "WhatsApp not connected — this journey won't send WhatsApp messages", rendered in both grouped and ungrouped rows). The auto-pause fires only on the TRANSITION to disconnected (it must not override a PM who deliberately re-activates afterward); the triangle warning stays visible the whole time the integration is disconnected so blocked entities remain identifiable. The user's directive "disini juga munculin error terus auto mati journeynya" ("here also show the error and auto-disable the journey") makes auto-deactivation + a visible error indicator the expected default for dependency loss, not just a passive warning. Confidence: 0.7
- When an integration is already connected, do NOT show any prerequisite/setup/connection-state warning — whether a "Requires X" capability notice (e.g. the "Requires WhatsApp Business API. WhatsApp messages must use an approved template." note, which only rendered when `whatsappConnected` was true) or a "not configured"/"not connected" warning tooltip/badge on a step card or channel option (e.g. the amber triangle + "WhatsApp not configured" tooltip on the journey step card, which rendered for every WhatsApp step regardless of connection). Such warnings only make sense when the requirement is unmet: gate them by the actual connection state so they show only while disconnected and hide when connected (the step-card tooltip was fixed to `v-if="isWhatsAppNotConfigured"`, i.e. `isWhatsApp && !whatsappConnected`, rather than removed). The user's phrasing across sessions — "if whatsapp connected dont show this" and "also if connected dont show this warning" — makes the rule explicit: connection-state warnings must never render while connected. Confidence: 0.75
- When the user pastes a third-party provider's API documentation (e.g. Channex's Taxes/Tax Sets reference) as the spec for a feature, treat it as the authoritative data model and mirror the API's entity fields, types, and enum values exactly — Channex `Tax` = `title`, `type` (`tax`/`fee`/`city_tax`), `logic` (percent/per_room/per_room_per_night/per_person/per_person_per_night/per_night/per_booking), `rate`, `currency`, `is_inclusive`, `skip_nights`, `max_nights`, `applicable_date_ranges` — rather than inventing a simplified local model (e.g. `kind`/`mode`/`value`). The user corrected a simplified `feesTaxes` model by pasting the full Channex docs. Recurred for Channex Rate Plans: the user pasted the full Rate Plans API reference with "cek listing detail terus pricing terus rate plan, sesuaikan dengan data ini", and the expected execution mirrored the whole entity — `title`, `sell_mode` (per_room/per_person), `rate_mode` (manual/derived/auto/cascade), occupancy `options` (occupancy/is_primary/derived_option/rate), `children_fee`/`infant_fee`, 7-element weekday restriction arrays (min_stay_arrival/min_stay_through/max_stay/closed_to_arrival/closed_to_departure/stop_sell), `inherit_*` flags, `auto_rate_settings`, `meal_type` — propagated through the type model, mock data, and every consuming UI (pricing tab, unit-type manager, reservation room section) in one pass. Re-confirmed when the user pasted the same Rate Plans reference again as the authority for the post-rework doc-verification pass. Confidence: 0.75
