# communication
- THE APPLICATION UI MUST BE IN ENGLISH: the user emphatically rejected Indonesian UI copy ("ui in english!!!!") when the assistant introduced Indonesian helper/description text into a form's checkbox group. This is a hard constraint on all user-facing copy/UI strings (labels, helper text, dynamic description sentences) — even though the user converses in Indonesian, the interface itself must be English. When adding UI copy, write it in English from the start; if Indonesian was introduced, proactively grep and convert all UI strings to English before reporting done. This contrasts with conversational replies, which may stay in Indonesian. Confidence: 0.8
- The user writes in Indonesian (terse shorthand, mixing in English technical terms like "no respon", "Submit report") and the assistant's Indonesian-language final summaries are accepted without complaint — e.g. the stacked-dialog bug fix was reported back as "Fixed. Ini bug dialog bertumpuk (stacked dialog)..." with Penyebabnya/Perbaikannya sections. Respond in the user's language; the earlier "always English" reading is contradicted by repeated Indonesian summaries across recent sessions. Confidence: 0.6
- The user gives terse UI-change directives in Indonesian shorthand (e.g. "booking history masukin ke cardnya" — "put booking history into its card"; "munculin info adult berapa child infant berapa" — "show the adult/child/infant counts"), often without naming the exact file or section; the assistant is expected to locate the target, pick the most sensible structural interpretation (e.g. CardHeader title + table body, with the child table dropping its own Card wrapper so cards don't nest), implement it, and commit — without asking clarifying questions (e.g. "cek page reservation cek reservasi emily chen, update struktur detail reservasi di reservasi lain seperti emily chen" — "check Emily's reservation and bring the other reservations' detail structure up to hers" — was executed directly: the assistant located Emily's record as the canonical shape and enriched all siblings; again in "di guests section ada button untuk edit guest info. tambahin timeline untuk reservasi itu apa aja yang dilakukan guest itu" — the assistant located the guests section in the detail sheet, added per-guest edit buttons and an activity timeline accordion, and populated timeline data for every reservation, all without clarifying questions). Again in "buat info kalau ada guest yang belum verify identity, terus ada section untuk liat document yang diupload guest kaya ID, selfie, dan signature, dan pdf agreement house rules" — the assistant built the identity-warning banner plus the Identity & Documents accordion (ID/selfie/signature/agreement with preview + download) directly, without clarifying questions. Recurred with "di detail owner juga di sheetnya" ("in the owner detail too, in the sheet") — a bare location pointer with no element specified that sent the assistant sweeping the whole OwnerDetailSheet component (grep all `new Date(...)` usages) for remaining date-format spots and normalizing every one, no questions asked. Confidence: 0.88
- For UI changes, the user often conveys the desired design by attaching a reference screenshot plus a short sentence in Indonesian (e.g. "untuk cleaning event cardnya kaya di gambar", "buat cleaning card eventnya tambahin handle by extrasauber, ini pngnya") rather than writing detailed written specs. Treat the screenshot as the source of truth and read it via the vision tool before touching code — this includes extracting brand colors, taglines, and logo styling from the image. The screenshot can also arrive with NO accompanying text at all (just "[Image #1]"): a visual annotation on the image — an arrow/circle marking a specific UI element (e.g. a "+ Add cost / revenue" button that looks out of place) — IS the entire instruction. Read the image, identify the marked element, infer the desired change from its position/context, implement it, and report back, without asking what the annotation means. Confidence: 0.88
- After completing a UI feature (or a bug fix), proactively state where the user can see it (exact route/page) and how to reproduce it (e.g. "Refresh http://localhost:3000/reservations, buka reservasi, klik ikon pensil di header") — the user will otherwise ask "ceknya dimana" ("where do I check it?") or "gimana caranya akses ini" ("how do I access this?") about a just-shipped feature. Answer such questions with a concrete click-path walkthrough covering both sides where applicable (e.g. the Contract + e-sign gate: portal login → owner email that hasn't signed → send secure link → open demo link → contract page → sign; plus the staff/admin path via the detail sheet's Contract tab). Offering to seed mock/demo data so the change is visible without manual setup is welcomed. Confidence: 0.75
- When the user phrases a reference-screenshot request as "buat kaya gini tapi lebih kreative" ("make it like this but more creative"), treat the image as inspiration to be enhanced, not a 1:1 template — keep the reference's core elements (ID/status chips, OTA logo, guest avatar + party size, property thumbnail, check-in/out with night count) and layer on creative enrichments (photo hero banner with gradient overlay, notes card, icon bubbles, price breakdown) while keeping previously-requested conventions (e.g. listing links stay black). Confidence: 0.7
- When the user pastes raw text content as the reference for what to display — analytics/stats layouts (labels, numbers, ordering) or actual data content like a booking note ("tambahin reservation notenya kaya ini" + pasted note text; later "cek data yang ada di emily chen tapi gaada di reservasi lain kaya booking notes ** THIS RESERVATION HAS BEEN PRE-PAID ** ..." + the full note) — treat the pasted content as the source of truth and mirror its labels, structure, and wording exactly in the implementation (the exact "** THIS RESERVATION HAS BEEN PRE-PAID ** ... BOOKING NOTE : ..." text was seeded and rendered verbatim, emphasis markers included). Confidence: 0.78
- Bug reports can arrive as a paste of the currently rendered page content (e.g. "yang muncul cuma Welcome back / Ni Kadek Deviani / Sign out / Documents … Annual Tax Report 2025 … Property Insurance Policy 2026 …") — the user pastes what's on screen instead of describing the problem. Diff the pasted output against the intended state to identify what's missing (here: the signed contract absent from the owner's Documents list) and fix the root cause rather than asking what's wrong. Confidence: 0.55
- During design reviews/brainstorming, the user approves presented sections with terse affirmatives ("1", "ya", "yes to all", "start", "goo" — a typo for "go") and expects the assistant to proceed with the recommended/default option — a bare "ya" to a section that contains an open sub-question (e.g. dialog vs. sheet for reservation detail) means "you decide, go with your recommended default", not a request to re-ask; a bare "start"/"goo" after a written spec means "approved — move to the next phase" (e.g. write the implementation plan). Confidence: 0.85
- The user can point at the exact UI element to fix by pasting its code comment/marker or raw HTML markup from the source (e.g. `<!-- Status chip (editable) -->` + "ini" = "this one"; or a pasted `<div>...</div>` block of a section header + "hapus ini") instead of describing the element — treat the pasted markup as the target and infer the desired change from the surrounding context rather than asking what they mean. Confidence: 0.7
- Boundary case to the "implement terse directives without asking" preference: when a terse request uses a vague umbrella term that could map to several distinct features — Indonesian (e.g. "buat setting untuk reservation" — edit info, housekeeping, smart lock, upsell, or status/payment settings) or English ("need to show which review can show on main page" — could mean a featured subset, all reviews, or two separate sets) — ask ONE clarifying question with concrete labeled options before building; the user engaged with the option list and picked the Recommended option each time ("Edit info reservasi"; the featured-toggle "Main Page" star approach). Specific one-line capability requests (e.g. "bisa add dan delete cleaning dari sana", "the review also need support multiple property") are still implemented directly without asking. Confidence: 0.68
- When the user provides a formal, numbered flow spec in Indonesian (e.g. "Flow 1: Tenant Connect Email (Setup — one-time)" describing entry point, options, steps, pending state, and verified behavior), treat it as the authoritative product spec: map the existing implementation against each spec point, close the gaps (tenant-slug default address, optional records, pending-state fallback, verified fan-out), update tests, and browser-verify — without asking clarifying questions. This is the structured-spec variant of the terse-directive pattern. Confidence: 0.6
- When the assistant surfaces open requirement questions with recommended defaults (e.g. "is inbound email in Phase 1 or not?"), the user resolves them with a terse Indonesian directive that names the requirement directly — "email masuk juga harus support" ("inbound email must also be supported") — instead of picking the numbered recommendation or answering "ya". Treat the directive as the scope decision and implement the full scope (receive pipeline, unmatched-sender edge cases, connection-state gating, tests, browser verification) without re-asking. Recurred again with "aku mau emailnya bisa nerima balesan juga" ("I want email to be able to receive replies too") — the assistant implemented the full inbound pipeline (match/unmatched handling, pop-up + Notification Center alert, tests, dev-server verification) without asking whether to build it. Recurred again when the user resolved the APOA-vs-AVS target fork with "dukung dua-duanya" ("support both") — a directive that names the scope (both systems, mock direct API submission) rather than picking a labeled option. Confidence: 0.72
- When requesting a tool/integration setup (e.g. installing the Kimi model provider), the user pastes the API key/secret directly inline in the chat message ("ini keynya sk-kimi-...") and expects the assistant to wire it up from there — no pointer to env files or credential stores is given. Treat the inline key as the source of truth for the configuration. Confidence: 0.6
- The user says "Claude Code" and can genuinely mean Anthropic's Claude Code, not this tool (Command Code) — e.g. "aku mau setting di claude code .claude setting" referred to the real `.claude` config files (`~/.claude/settings.json` user-level, `.claude/settings.local.json` project-level), where a Kimi/Moonshot provider config (ANTHROPIC_BASE_URL → api.moonshot.ai/anthropic, ANTHROPIC_AUTH_TOKEN, model `kimi-k3[1m]`) was found already installed. When the user explicitly mentions ".claude", check the real Claude Code settings files first rather than assuming a product-name mix-up or offering a Command Code setup (the earlier "install kimi di claude code" read as Command Code was a misread — the target config was Claude Code's user-level settings). Confidence: 0.55
- After a feature is implemented, the user asks "jelasin gimana flownya" ("explain how the flow works") to understand the end-to-end behavior of what was just built — answer with a structured walkthrough (setup/entry point → how the feature gets triggered → pipeline stages in order → matching/filter conditions → explicitly what is real vs mock/config-only) rather than a recap of files changed or code. Confidence: 0.55
- The user pastes raw API documentation (e.g. Channex's Taxes/Tax Sets reference, later Channex's Rate Plans reference) as the spec for a feature and expects the assistant to infer the intent and apply it — resolve ambiguity not by asking, but by mapping the pasted entity model onto the feature at hand and propagating it through the data model + UI in one pass. The accompanying instruction may be absent or terse, naming the UI path to align in order (e.g. "cek listing detail terus pricing terus rate plan, sesuaikan dengan data ini" — check listing detail, then pricing, then rate plan, align with this data), which maps to tracing the feature through each of those surfaces. Confidence: 0.62
- Follow-up scope revisions to a just-shipped feature stated in terse English, generalizing a previously-single model ("for property can be multiple not only single"), are implementation directives, not design discussions — the expected execution is a full model propagation without clarifying questions: change the type (`PropertySelection.propertyId: string | null` → `propertyIds: string[]`), update every consumer (multi-select property step with per-property room toggles, review filtering by union of mapped listing ids with dedupe, preview summary, page state/validation), extend tests via RED→GREEN, verify (unit tests, scoped lint, filtered typecheck, dev-server smoke test), and commit. Recurred for integration accounts when the user generalized the guest-registration module from one account per provider to multiple accounts ("i can also connect multiple avs and apoa account and assign to listing") — executed directly as a full model propagation (connections array, per-listing account ownership with conflict detection, report routing to the owning account, both settings panels rewritten) without clarifying questions. Confidence: 0.65
- A question like "apakah ini juga konek ke page reservation?" ("does this also connect to the reservation page?") about a just-shipped feature is a completion directive, not a request for a yes/no status report: trace both the data layer and the UI surfaces end-to-end, and if only the data layer is wired (e.g. check-in auto-syncs a registration record) while no UI exists on the target page, treat that as "koneksinya baru separuh" (half-connected) and close the gap in the same pass — add the missing UI surface mirroring the closest existing section pattern (a "Guest registration" accordion in ReservationDetailSheet following the Smart Lock/Housekeeping accordions: status badges with variant mapping, not-connected empty state with a "Connect in Settings" link, and a "View all" link), then lint + typecheck + smoke-test. Answering "not yet" without completing the connection is not acceptable. Confidence: 0.6
- When the user hands a feature spec as a file path plus a terse directive ("/path/to/elev8_owner_portal_flows.md tolong implement fitur ini"), treat the file as the authoritative spec: read it first, map the existing implementation against it (much may already exist — here the PRD was already marked V1+V2 complete, so a large part of the spec was built), and implement only the true gaps. For a spec spanning many flows/modules, ask scope questions with labeled options (which flows now, high-season source, approval UI placement, cleaning wiring, download behavior) before writing the plan — the user picked the recommended options each time and had all flows implemented. Confidence: 0.6
- Follow-up questions about a just-shipped feature that ask where its data lives ("terus dimana disimpan dokument contractnya" — "so where is the contract document stored?") are storage-architecture questions, not bug reports: answer with concrete layers — the state/composable holding the record (e.g. `elev8-owner-contracts`), the document layer it links to (e.g. Document Center `elev8-owner-documents` with category/visibility scoping), where each side reads it from (owner portal vs. staff Document Center), and explicitly flag what is NOT persisted into the downloadable artifact (e.g. the drawn signature stored on the record but not embedded in the downloaded contract document) with an offer to wire it up — rather than a recap of files changed. Confidence: 0.55
- A variant of the provenance question: when the user asks where a displayed value comes from ("dmana dapet info Operational fee itu" — "where does the operational fee info come from?"), they want a full source trace distinguishing real configuration from hardcoded/seed placeholders — enumerate every location the value appears (seed records, generation handlers, sign-time logic), state plainly that it is hardcoded and not editable, call out the gap against the product spec (PRD said per-owner configurable but no UI/data source exists), and offer to build the missing configuration flow. Answering "it's from the seed" without the trace, the hardcoded-vs-config flag, and a fix offer is not enough. Confidence: 0.55
- The user can convey a format/design spec by pasting a FILE PATH to an example output document with a terse pointer and no further explanation (e.g. "ini contoh pdf statementnya" + a path to a sample owner statement PDF) — treat the referenced document as the authoritative format spec: extract its structure (the assistant used `pdftotext` with a Python pypdf fallback to read the binary PDF), identify its sections (branded header, property/owner info, split boxes, large payout figure, summary, per-item table), and mirror them in the generator without asking what format is wanted. This is the document-file variant of the reference-screenshot and pasted-text-reference patterns. Confidence: 0.6
- When the assistant's exploration balloons past the immediate ask (e.g. after repeated context-length errors mid-multi-file implementation), the user cuts scope with a terse directive ("cukup cek isi pdfnya aja" — "just checking the PDF's content is enough") — comply by stopping the broader implementation and completing only the minimal read-only step requested (extract and report the PDF's contents), rather than continuing to pull more data files or build code. Terse scope-narrowing directives are instructions to shrink, not to proceed with the original plan. Confidence: 0.6
- When the user asks for a summary of what was changed in order to send it to an external stakeholder (e.g. "buat summary apa aja yang aku ubah ... untuk kirim ke Reto"), they want ONLY the big features ("fitur besar aja") — a high-level, user-visible feature list (numbered sections with bold headings and short bullets) rather than a full change log of files, types, or implementation details — and offering a shorter 3-4 line condensed version afterward is welcomed. Confidence: 0.6
- External-facing written deliverables (summaries/reports for a stakeholder like Reto) should be in English even though the working conversation is in Indonesian — the user switched a just-delivered Indonesian summary with a terse "in english" and expected the same content re-rendered in English in place, not a re-ask or an Indonesian/English mix. Confidence: 0.55
- When the user pastes a FORM-FIELD spec (field names with `*` required markers, `(optional)` annotations, and placeholder examples — e.g. the AVS connection fields: "Provider *", "Account Name · e.g. Elev8 Tourism Office (optional)", "Kurverwaltung ID * · e.g. 12345", "Firma ID * · e.g. 6789", "Objekt ID * · Objekt ID for direct bookings", "Objekt ID (Airbnb) · Objekt ID for Airbnb bookings (optional)"), treat it as the authoritative source of truth for BOTH the data model and the connect form: mirror the field names/order exactly, honor the required-vs-optional markers as the validation rules (only `*` fields gate submission; `(optional)` fields are omitted when empty), and use the pasted placeholder examples verbatim. This is the data-model/form variant of the pasted-content-as-reference pattern. Confidence: 0.7
