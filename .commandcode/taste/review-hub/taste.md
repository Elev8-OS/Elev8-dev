# review-hub
- Use "stay report" (not "SOR" or "Stay Operational Record") as the user-facing terminology in the Review Hub UI and PRD documentation. Confidence: 0.65
- Booking.com does NOT support host reviews of guests at all — only Airbnb supports host reviews per Channex API. Confidence: 0.80
- For Airbnb double-blind reviews: host submits review (public + optional private feedback + ratings) within 14 days which reveals the guest's review immediately; if guest hasn't reviewed, show "guest hasn't reviewed" message. After 14 days, if guest submitted a review, host can reply to it (30-day reply window). Confidence: 0.80
- Host review submission and reply-to-guest are separate flows with distinct statuses — do not conflate them into a single "reply_status". Confidence: 0.70
- Display review ratings as numbers (not stars) for all sources, including converting Airbnb 5-star to a 10-point numeric scale. Confidence: 0.70
- Use consistent numeric scoring format (X/5) across all related badges/metrics — avoid mixing count-based display with score-based display. Confidence: 0.65
- In FeedTable Property column: show unit info (e.g., "Kingbed · Master Suite") for multi-unit records, but display "single unit" text (not location) for single-unit records. Confidence: 0.70
- All review scores are normalized to Channex 0-10 scale internally; use getDisplayScore() and getDisplayMax() to convert to channel-native display (Airbnb ÷2, Booking.com as-is). Confidence: 0.75
- Use "respect_house_rules" (not "house_rules") as the field name for host review guest ratings, aligned with Channex API. Confidence: 0.80
- Use scores: ScoreCategory[] (array of {category, score}) instead of per-channel rating objects for guest review categories, aligned with Channex data model. Confidence: 0.75
- Channex reply-to-review endpoint (POST /reviews/:id/reply) only accepts plain text — tags are only available on the host-review-of-guest endpoint (POST /reviews/:id/guest_review). Confidence: 0.80
- Guest/booking status uses a fixed semantic color scheme across the app — review cards and reservation status chips alike: Grey = Unverified (guest hasn't filled info), Green = Verified (info filled, look for "Thank you for filling your guest guide" message), Orange = Checked-in, Blue = Checked-out, Grey with strikethrough line = Cancelled, Black = Manual Block (calendar block, not a reservation — must carry a block reason), Yellow = Inquiry (not yet confirmed, awaiting payment; check for auto-remove setting after XX hours). The user restated this exact spec when asked to recolor reservation status chips, confirming it is an app-wide convention, not a review-hub-only detail. Confidence: 0.95
- Reviews with only a rating and no written feedback need distinct status treatment (not the same as "needs_reply") — the rendered card surfaces the rating but skips the review-text block and shows Reply/HostReview panels normally. Confidence: 0.75
- In Review Hub analytics, display all review scores on a single normalized 0-10 scale (max 10) for every channel — show the Channex 0-10 value as-is rather than converting to channel-native display (Airbnb ÷2 to /5, Booking.com /10). The user explicitly asked to drop the per-channel scale conversion in analytics. Confidence: 0.8
- Review Hub analytics categories are channel-aware: Airbnb → Value / Location / Check-In / Accuracy / Communication / Cleanliness; Booking.com → Value / Cleanliness / Location / Comfort / Facilities / Staff. Confidence: 0.7
- Review Hub analytics must use a single deterministic historical seed dataset as the single source of truth — KPIs, YoY trend charts, and category performance all computed from the same records, so stat cards and charts never tell different stories (a separate mock time-series disconnected from the live KPIs was explicitly rejected). Confidence: 0.8
- In Review Hub analytics, WoW/YoY deltas must be computed from the actual filtered data (current period vs the previous same-length period / same week last year) so they react to filters — never static numbers from a hidden mock series. Confidence: 0.8
- In Review Hub analytics, offer period presets (7D / 30D / 90D / 12M / Custom) that drive the date range and filter everything (KPIs + charts + categories); picking a custom date range switches the preset to "custom". Charts must actually re-render to the selected date window when the filter changes — the user reported a full-range trend chart ignoring the date filter as a bug, and verified the fix by switching presets (30D → 7D) and confirming the x-axis narrows. Confidence: 0.8
- In Review Hub analytics, top negative tags (and per-category tags) percentages are % of the total reviews in the filtered set — not % of tag mentions. Confidence: 0.75
- In Review Hub analytics, listing status counts (Good or Educational / Warning / At risk / Removed) are per-listing counts shown as X/total listings, derived from each listing's average rating in the filtered period — not counts of reviews. Confidence: 0.7
- In Review Hub analytics, category-tag sentiment (top positive/negative tags per category) should be a grouped horizontal bar per category — green positive bar on top, red negative below, bar length = mention count, tag label beside it (mirroring the top-3 positive/negative-per-category structure the user originally pasted) — not a bubble chart. Long tag labels should stay fully readable: widen the label column so labels render in full (ellipsis + tooltip only as a last resort). Confidence: 0.7
- In Review Hub analytics, Category Performance should include a radar/spider chart (6 categories as radial axes on a 0-10 scale, concentric grid rings, axis labels, filled polygon in the chart's explicit color, value at each vertex) alongside the per-category score cards — the user explicitly requested a radar chart for category performance. Chart fill/series colors are explicit and theme-independent (indigo), not inherited from the theme primary (the user rejected the yellow `--vis-primary-color` rendering). Likewise, analytics text colors are explicit rather than inherited theme primary: the active date-range label was changed from yellow `text-primary` to black `text-foreground`, but the "View listings" action link keeps theme-primary (yellow) — the user said "view listing gapapa kuning" ("View listings staying yellow is fine") and reverted its recolor. Confidence: 0.75
- In Review Hub analytics, the trend-chart comparison baseline must be user-selectable via a filter control ("vs last year" / "vs previous period"), not hardcoded to last year — the user explicitly asked for a comparison filter; switching mode should update chart titles, descriptions, legends, and KPI deltas together. Confidence: 0.65
- Guest review translation target language follows the existing Review Automation Language setting (en/de/fr/id/es/it/pt, default en) — the user chose "Ikut setting Language" over hardcoding Indonesian or English, for consistency with the AI-reply config that already uses this setting ("bahasa user" = whatever the app Language setting says). Confidence: 0.7
- Guest review translation UX is a per-review toggle (translate on demand; toggle back to the original text) placed next to the review text / language label, mirroring the existing Inbox mockTranslate pattern — not a global auto-translate toggle. Confidence: 0.7
- Mock/demo translation should reuse the app's existing mockTranslate pattern (delay + lookup map with an "[Translated]" prefix fallback) rather than introducing a real LLM/translation library — the user chose "Mock translate (Recommended)" over real LLM translate, consistent with the mock-dashboard approach used in Inbox. Confidence: 0.7
- Guest review translation results should be persisted on the ReviewRecord itself (new optional + nullable fields `translated_content` and `translation_language`, set through `updateReviewRecord`) rather than kept in ephemeral local component state — the user chose Approach B over a local `ref` in DetailGuestPanel, so a translation survives drawer close/reopen and mirrors the existing `reply_text` persistence pattern (once translated, never re-translated). Confidence: 0.7
- The translate feature must cover ALL content surfaces in Review Hub, not just the stored guest review text — AI auto-generated drafts (the AI reply-to-guest in `ReplyPanel` and the AI host-review draft in `HostReviewPanel`) also get the same per-draft translate toggle, because the user explicitly extended the scope with "auto generate by ainya juga harusnya ada translatenya" ("the AI auto-generate should also have translation"). Implement it as a shared `translateText` helper in `useReviewHub.ts` reused by both panels (same mock pattern, same Review Automation Language setting, same translate ↔ show-original toggle) rather than duplicating per-component logic. Confidence: 0.7
- AI-generated drafts in Review Hub (AI reply in `ReplyPanel`, AI host review in `HostReviewPanel`) also get a "Rephrase" action that rewrites the generated draft text — the user asked "tambahin button rephrase samping regenerate, fungsunya merephrase kata kata yang di generate" ("add a rephrase button next to Regenerate; it rephrases the generated words"), and it was implemented as a shared mock `rephraseText` helper in `useReviewHub.ts` (delay + synonym swap) reused by both panels, consistent with the shared `translateText` helper. Rephrase acts on whatever text is currently shown (original or translated draft) and resets the translate view so the two features don't mix states. Confidence: 0.7
- Manual reviews must have feature parity with regular hub/channel reviews: they can be featured on the main page via the same star "Main Page" toggle, show a "Main Page" badge in both the inline chips and the live preview, count toward the featured total (preview header + PreviewStep summary), persist on the Website stub via `featuredManualReviewIds` with edit-mode restore, and have their featured flag cleared automatically when the manual review is removed. Parity also covers listing association: every manual review carries a required `listingId` (mirroring how hub reviews belong to a listing) — the add-review dialog gained a Listing dropdown restricted to listings mapped to the currently selected properties, validation requires it, and the listing name is shown in the chips and live preview. Both requests came as terse follow-ups — "manual review juga bisa dimasukin ke main page" and "di manual review juga harus select listing mana" — and were implemented by mirroring the existing hub review patterns. Parity extends to OTA channel too: a terse follow-up "di manual juga bisa milih channel OTAnya" led to a required `channel: ReviewSource` field on ManualReview ('airbnb' | 'booking_com' | 'direct') with a Channel dropdown in the add dialog (default Airbnb), reusing the review-hub `channelLabels`/`channelIcons` constants so labels/icons stay consistent with hub reviews, and the channel badge shown in both the chips and the live preview. Confidence: 0.78
