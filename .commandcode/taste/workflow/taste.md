# workflow
- When asked to "check/explore a feature" with no specifics (or asked "apa lagi celahnya" — what other gaps remain), proactively map the whole feature end-to-end (feature files, mount points, data, tests, wiring) and surface anomalies (e.g. wrong handler wired to a button, dead config, promised-but-unimplemented features, missing UI triggers, data-integrity risks) as a prioritized list ranked by business impact, with a recommendation and an offer to fix. Expect iterative rounds: the user keeps asking for fresh gap passes until satisfied. Confidence: 0.86
- Push to GitHub after completing each feature chunk (the user asked to "push ke github" repeatedly mid-session, and again with a terse "push ke github" right after the review-hub sort fix). Confidence: 0.97
- Update CLAUDE.md alongside pushes to keep project context current. Confidence: 0.92
- Commit first, then push (`git commit` → `git push`). Confidence: 0.70
- Check git status (uncommitted/untracked) before pushing. Confidence: 0.85
- Brainstorm and discuss design before implementation. Confidence: 0.82
- During feature brainstorming, ask clarifying questions ONE at a time, each with concrete labeled options and a short description (not open-ended lists) — the user engaged with and answered each sequential question in this format. Confidence: 0.6
- The user engages with interactive browser mockups of layout options during brainstorming (visual companion) and picks options by clicking; when the browser display appears stalled, a terse "run again" means re-check the companion server state and re-push the content, then continue. Confidence: 0.5
- Use test selectors such as stable `data-testid` attributes and accessibility state attributes like `aria-pressed` for interactive controls. Confidence: 0.85
- Prefer test-driven implementation with an explicit RED → GREEN loop for feature work. Confidence: 0.85
- During verification, separate failures caused by repository/environment baseline issues from regressions introduced by the current work, and report both clearly. Confidence: 0.88
- Prefer scoped local test runs when the repository's full test discovery includes generated or worktree tests; still run the full suite when practical to detect regressions. Confidence: 0.82
- When full-suite test discovery picks up stale sub-agent worktrees (`.claude/worktrees/**`) and causes mass failures, fix the root cause by excluding them in the vitest config (`exclude: ['**/.claude/worktrees/**']`) rather than deleting the worktrees or working around them — the worktrees may hold the user's in-progress work. Confidence: 0.6
- Treat focused tests, typecheck diagnostics filtered to touched files, lint, and code review as acceptable manual-flow evidence when the dev server is unstable or cannot boot reliably. Confidence: 0.78
- Review the whole branch against its base and inspect the final diff before declaring work merge-ready. Confidence: 0.82
- Commit follow-up fixes separately with focused conventional commit messages rather than silently amending prior feature commits. Confidence: 0.75
- Exception: when fixing CI/lint failures on a still-unmerged feature branch right before merging, prefer `git commit --amend` + `git push --force-with-lease` over a separate fix commit — keeps the PR history clean and avoids polluting main with a "fix lint" commit. Confidence: 0.82
- When "merge" is requested on a feature branch with an open PR, check CI status first; if lint/typecheck is failing, fix the issues, amend + force-push, then merge via `gh pr merge --merge --delete-branch-remote`. Confidence: 0.85
- When a PR's CI lint is red, first separate errors the PR introduced from pre-existing baseline errors (grep the CI logs for the touched files; confirm main's own CI runs are already failing). Fix only the PR's own errors; then, with the rest proven pre-existing, ask once with a recommended "merge without waiting for lint" option — the user will choose to merge anyway rather than have the pre-existing lint backlog (e.g. 4542 issues in `tests/`) fixed first. Confidence: 0.78
- The user may compress the PR-open and merge steps into a single terse directive ("open pr and merge") — execute the whole chain autonomously (check gh CLI / existing PR → create PR → check mergeability + CI → fix own errors → merge + delete branch) without pausing for confirmation after the PR is opened. Confidence: 0.55
- Prefer scoped lint runs on only the changed files (e.g. `pnpm exec eslint <changed-files>`) over the full project lint when the full lint is too slow — then run the scoped re-lint to confirm fixes. Confidence: 0.78
- Prefer discovering and fixing missing exports or typecheck regressions uncovered during final verification before finalizing a feature. Confidence: 0.86
- Use `NODE_ENV=test` explicitly for local Vitest runs when the test environment otherwise alters Vue Test Utils behavior or emit capture. Confidence: 0.78
- Model data at the granularity the end user actually selects/redeems, not at a parent/aggregate level. Example: when a "Spa" service has multiple items (1hr, 2hr, body scrub), the picker must let the operator pick specific items — picking the whole service is wrong because the unit of redemption is the item. Confidence: 0.88
- When explaining requirements, prefers concrete real-world examples (e.g. "spa have 1 hour, 2 hours") over abstract spec terminology. Worth asking for or providing an example when intent is ambiguous. Confidence: 0.72
- Exclude auto-managed files (e.g. taste files, untracked local scratch files) from git commits; only stage files explicitly related to the feature being shipped. Confidence: 0.9
- Use a dedicated feature branch with conventional `feat(scope):` naming (e.g. `feat/promo-free-upsell-item-selection`) for non-trivial changes before pushing. Confidence: 0.80
- When opening a PR, mirror the commit message structure and include a dedicated "Verification" section listing the typecheck/lint/test commands that were run. Confidence: 0.82
- After running typecheck on a large project, filter the output to only the touched files (e.g. `vue-tsc --noEmit 2>&1 | grep -iE "ListingPicker|..."`) to separate regressions introduced by the current change from pre-existing baseline errors — then explicitly state that no new errors were found. Confidence: 0.85
- When asked to move/relocate a feature across modules, use `ask_user_question` with concrete placement options (each with a short preview) before touching code — even when the task feels straightforward, placement decisions have lasting structural impact. Confidence: 0.85
- After first implementation of a UI move, do a follow-up visual/scope review pass and ask another clarifying question if the result needs a second iteration (e.g. layout tightening, prop additions) rather than silently refactoring. Confidence: 0.78
- When reusing a full-page list/table component inside a smaller context (sheet tab, popover, card), prefer adding a `compact` boolean prop to the existing component over rewriting an inline version — keeps a single source of truth and lets the original use site stay unchanged when the prop defaults to `false`. Confidence: 0.85
- When a bulk/throttled test exceeds the default test timeout (e.g. 50-item bulk sync with per-item network delay), reduce the fixture size to ~10 items rather than raising the timeout — keeps the suite fast while still verifying the throttle pipeline. Confidence: 0.80
- For health-state integrations: model three discrete states (`connected` / `needs_attention` / `disconnected`) with a separate `flagNeedsAttention(reason)` / `clearNeedsAttention()` / `reconnect(key)` API rather than overloading `disconnect()` — preserves the user's data while flagging the failure for Admin action. Confidence: 0.82
- When seeding domain data (e.g. listings, bookings) that should plug into a third-party integration's mock fixtures, cross-reference guest names / IDs / revenue values between the new domain data and the integration's existing seed data so the end-to-end demo flow remains consistent. Confidence: 0.82
- When the user asks to seed demo data "with all the data" (e.g. "seed 1 reservation with all the data"), create ONE fully-populated record rather than several sparse ones — wire every relation the UI can render (conversation, payment request, guest guide, upsell orders, activity spanning all event types, tags/notes) to real existing datasets so every section lights up with no empty states, and verify each section renders. Confidence: 0.7
- Standard endpoint cadence at the end of a feature chunk: user asks to push → commit + push to GitHub branch → user says "yes PR" → open the PR → user says "merge" → merge PR (after CI green) + delete the remote branch. Don't volunteer intermediate previews unless asked. Confidence: 0.85
- When `pnpm install` skips ignored build scripts needed by the toolchain (esbuild, @parcel/watcher, vue-demi, maplibre-gl, etc.), don't rely on interactive `pnpm approve-builds` in the agent shell — set `onlyBuiltDependencies` non-interactively via `pnpm config set --location=project onlyBuiltDependencies "[...]"` (writes to project `.npmrc`/`pnpm-workspace.yaml`), then run `pnpm rebuild` to compile the native binaries. Confidence: 0.82
- When a background dev server exits immediately with code 1 and an empty log, re-run the command in the foreground to capture the actual error output before diagnosing. Confidence: 0.72
- When the environment's Node version doesn't match the project's `engines` (e.g. system Node v25 vs required 22.x), don't block on it — proceed if it works (treat as a warning), but flag the mismatch to the user and suggest a version manager (e.g. nvm-windows) for full compatibility. Confidence: 0.68
- When a documented/mocked feature needs a "real" version, implement the code first and update the PRD/doc afterwards to reflect the actual implementation (user chose "implement first, PRD follows"). Confidence: 0.65
- When a design choice carries a data-integrity risk (e.g. one shared sync flag across integrations), re-ask the question with a recommendation even if the user already picked the simpler option — the user will revise when shown the tradeoff. Confidence: 0.6
- When `git pull` is blocked by local uncommitted changes, preserve the user's work and git state: `git stash push` (with a descriptive message like `wip-before-pull`) → `git pull` → `git stash pop`; then confirm the user's real source changes were restored and don't commit anything on the user's behalf — the working tree should reflect the pre-pull state plus conflict resolutions, not new commits. Confidence: 0.74
- When merge conflicts appear in auto-managed `.commandcode/` files (settings allowlists, taste learnings) and are purely additive, resolve by keeping both sides (merge the upstream and local entries) rather than discarding either; afterwards grep for leftover conflict markers before dropping the stash entry. Confidence: 0.7
- When the user hits an error/validation message and asks what it means (e.g. "apa artinya" for "Ownership for listing ... would exceed 100%"), first trace the exact message string in the code and read the enforcing logic (e.g. `validateOwnership()` in `useOwners.ts`) before answering; then explain the plain-language meaning, the rule/flow behind it (e.g. the 2-pass ownership-percentage validation), concrete fixes, and cite file:line references. Explain the error rather than jumping into code changes unless asked to fix. Confidence: 0.65
- When the user gives a terse Indonesian UI-change directive without naming the exact element (e.g. "ubah warna textnya ke hitam" after charts were fixed), don't ask which element — infer the scope from recent context (what was just shown/rejected, e.g. the theme-yellow "View listings" link), grep the area for the offending pattern (e.g. `text-primary`), and apply the change consistently to every remaining occurrence so the whole surface is uniform instead of leaving a mix. Confidence: 0.6
- When the user reports a bug in a terse one-liner with no repro steps — Indonesian ("bug gabisa klik add another", "pas create operation gabisa assign extrasauber", "bug pas ubah filter date chartnya ga berubah", "bug di reservasi detail gabisa klik edit", "ngga berfungsi sortnya") or English ("bug sheet padding") — don't ask for clarification upfront; investigate autonomously: read the offending component, match the app's existing convention, then verify (typecheck filtered to the touched file + dev log) before committing the fix and stating where to see it. Confidence: 0.92
- To prove that lint/typecheck errors on touched files are pre-existing (not introduced by the change), use `git stash` (or a baseline commit) to run the same check before and after the change and compare error counts per file (e.g. baseline 54 errors vs current 56 → isolate the 2 new ones, then fix only those) — the user expects the assistant to distinguish its own regressions from the repo's pre-existing lint backlog and to leave baseline errors alone. Confidence: 0.7
- Terse Indonesian feature requests — whether they include pasted reference content (e.g. "tambahin reservation notenya kaya ini" + the exact note text) or just one line describing the desired capability (e.g. "munculin tanggal berapa next cleaning trus bisa add dan delete cleaning dari sana") — are implementation directives, not design discussions: implement directly without asking clarifying questions or routing through the planning skill, mirroring pasted content exactly and inferring scope/location from the phrasing (e.g. "buat expandable" → the price breakdown was converted to an accordion). Confidence: 0.7
- When a component wraps shadcn inputs that use `useVModel(..., { passive: true })`, Vue Test Utils `setValue`/`trigger` on the mounted component may not produce `update:modelValue` emits, so direct-mount tests can't capture state changes. Drive the scenario through the full dialog flow instead (mount the dialog, navigate to the step, dispatch native input/change events on `document.body` elements) — that path is proven to work. Confidence: 0.6
- When verifying stateful form behavior in a real browser, drive inputs and assert values via native DOM events through `eval` (set `.value`, dispatch `input`/`change` with bubbles, then read values back), because `agent-browser fill`/`snapshot` can silently no-op or omit values when element refs shift or inputs sit inside dialogs. Confidence: 0.55
- When reproducing a form/popover bug with browser automation, repeated automated clicks within the same page load can corrupt Reka UI popover/sheet state (triggers stop committing selections, refs shift), producing false "still broken" results — reload the page for a clean state and re-run the exact minimal flow before concluding a bug is real or a fix is ineffective. Confidence: 0.7
- When the user insists a reported bug still exists but clean-load browser testing shows the flow works, present the verification evidence, attribute the residual failure to stale bundle/state, and give hard-refresh + `pnpm dev` restart steps; ask for a screenshot of the exact failing step if it persists on their side. Confidence: 0.6
 and fix it across ALL records of that type — the user expects a systematic sweep, not a one-off fix ("why cleaning 6 aug Ravi Sharma cannot edit?... please check ALLL!!!!!!!"). Confidence: 0.75
- "Fallback"/"revert" means restore the previous working state precisely — the user audits reverts and flags a wrong restore ("salah revert"), so compare the reverted UI against what existed before the change. Confidence: 0.6
- Prefers new features structured as standalone modules following the app's dominant pattern — a `components/<module>/` directory, a `composables/use<Module>.ts` with `useState`-based state, and dedicated pages — over reusing another module's data directly or thin wrappers over existing datasets (user chose the standalone-module approach for the reservations feature over building on inbox/finance data). Confidence: 0.5
- Uses the writing-plans skill workflow: after a spec is approved, load the skill, study the real codebase patterns in detail (composable, page layout, table, mock data shapes, test setup), then write a task-by-task plan with complete code and per-task verify-and-commit steps, saved as a dated markdown file in `docs/superpowers/plans/` following the repo's naming convention. Confidence: 0.6
- Before writing an implementation plan, read the actual existing files (composable, page, data, tests, config) so the plan contains accurate, complete code matching repo patterns rather than placeholders or approximations. Confidence: 0.6
- When a plan's code has a genuine type/API mismatch with the actual repo data (e.g. a component expecting `UpsellItem[]` but the data carries upsell IDs), fix it by wiring to the codebase's established pattern/source of truth — resolve IDs against the canonical data composable exactly as existing components do (e.g. inbox `ReservationUpsells` uses `useUpsellOrders()` with `linkedOrderIds`) — rather than forcing the plan's shape or inventing a new approach; document the deviation in the commit message body and the progress ledger. Confidence: 0.72
- To definitively separate pre-existing test failures from regressions, don't infer from file names — create a `git worktree add` at the pre-feature baseline commit, install deps, run the full suite there, and compare exact file/test failure counts against the feature branch before reporting "all failures pre-existing" (e.g. baseline 16 files/38 tests == branch 16 files/38 tests, branch adds passing tests, no new failures). Confidence: 0.7
- Maintain the SDD progress ledger (`.superpowers/sdd/progress.md`) as the running source of truth during multi-task plan execution: after each task mark it done with the commit hash, typecheck status, and explicit notes on plan deviations and bugs found (subagent failures, rewritten components, baseline-tolerated type errors) — update it before final verification. Confidence: 0.65
- When the user asks to "audit UI" on a specific page ("audit ui di page X dan perbaiki"), treat it as a directive to run a comprehensive audit — missing imports/component resolution, broken-image fallbacks, heading/copy casing consistency, empty/loading states, responsiveness, plus lint and dev-log warnings (e.g. "Failed to resolve component") — then fix every finding autonomously, verify with scoped lint + typecheck + rendered-output check, and commit in one pass rather than asking what to look for. Confidence: 0.6
- When the user asks how to perform an action on a just-shipped feature (e.g. "cara editnya gimana?" right after a read-only guest list was added), don't just explain — treat it as a signal the UI lacks that capability and implement the missing edit flow (per-item edit/delete buttons + add form inside the existing edit dialog), then verify and commit. The user expects view and edit capabilities to ship together or be added promptly on request. Confidence: 0.65
- When cleanup feedback is ambiguous (e.g. "redundant text"), apply the conservative interpretation: trim the duplicated text within existing sections rather than deleting the sections themselves. Deleting whole features/sections on ambiguous wording backfires — the user objected "kenapa ilang semua" ("why did everything disappear") when the Activity/Payment Requests/Upsells/Notes/Quick-links sections were removed from the guest profile, and the sections had to be restored. If over-deletion happens, restore the removed content promptly and acknowledge the mistake rather than defending the deletion. Confidence: 0.75
 "kenapa ilang semua" ("why did everything disappear") when the Activity/Payment Requests/Upsells/Notes/Quick-links sections were removed from the guest profile, and the sections had to be restored. If over-deletion happens, restore the removed content promptly and acknowledge the mistake rather than defending the deletion. Confidence: 0.75
