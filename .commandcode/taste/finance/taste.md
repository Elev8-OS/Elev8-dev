# finance
- Cost accounts should use a single default line item (not split by type) since cost types are handled via tags. Confidence: 0.70
- For Booking Revenue double-entry tables: each line item must have separate Debit and Credit account columns/selectors, not a single account with a pre-assigned debit/credit indicator. Confidence: 0.65
- No charts or progress bars — use full data tables only. Confidence: 0.90
- Use toast feedback for all user actions (create, rename, delete, toggle, save, move). Confidence: 0.85
- Use confirmation dialogs for destructive actions (delete, duplicate). Confidence: 0.80
- Header currency must dynamically reflect the tenant account's currency setting (currently CHF). Confidence: 0.70
- New accounting integrations follow a fixed structural pattern: (1) `app/components/finance/data/<id>.ts` for chart-of-accounts + helpers, (2) `app/components/finance/data/<id>-invoices.ts` for invoice types + seed fixtures, (3) `app/composables/use<Id>.ts` composable with `useState`-based state, (4) `app/components/finance/<Id>Integration.vue` + `<Id>Logo.vue`, (5) `tests/composables/use<Id>.spec.ts` covering connection lifecycle + pipeline + edge cases. Then wire into `integrations.ts` registry, `IntegrationsTab` componentMap, `useIntegrationAccounts` defaults, and `useListingMappings`. Confidence: 0.88
- For integrations with VAT/tax rate constraints (e.g. Lexware's 0/7/19 bands), reject unmappable invoices into a `needs_mapping` status with a clear `needsMappingReason` field rather than silently dropping them — Finance needs visibility. Confidence: 0.85
- Bulk historical sync operations must throttle to match the third-party API's rate limit (e.g. Lexware's 2 req/sec) with a progress bar in the UI; surface the throttle rate explicitly in user-facing copy. Confidence: 0.82
- Account-mapping config dialogs for integrations use a Tabs layout with one tab per transaction type (Booking Revenue / Upsell / Costs / City Tax) — matches the existing Bexio/Jurnal pattern. Confidence: 0.78
