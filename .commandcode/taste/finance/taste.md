# finance
- Cost accounts should use a single default line item (not split by type) since cost types are handled via tags. Confidence: 0.70
- For Booking Revenue double-entry tables: each line item must have separate Debit and Credit account columns/selectors, not a single account with a pre-assigned debit/credit indicator. Confidence: 0.65
- No charts or progress bars — use full data tables only. Confidence: 0.90
- Use toast feedback for all user actions (create, rename, delete, toggle, save, move). Confidence: 0.85
- Use confirmation dialogs for destructive actions (delete, duplicate). Confidence: 0.80
- Header currency must dynamically reflect the tenant account's currency setting (currently CHF). Confidence: 0.70
- New accounting integrations follow a fixed structural pattern: (1) `app/components/finance/data/<id>.ts` for chart-of-accounts + helpers, (2) `app/components/finance/data/<id>-invoices.ts` for invoice types + seed fixtures, (3) `app/composables/use<Id>.ts` composable with `useState`-based state, (4) `app/components/finance/<Id>Integration.vue` + `<Id>Logo.vue`, (5) `tests/composables/use<Id>.spec.ts` covering connection lifecycle + pipeline + edge cases. Then wire into `integrations.ts` registry, `IntegrationsTab` componentMap, `useIntegrationAccounts` defaults, and `useListingMappings`. Confidence: 0.88
- For integrations with VAT/tax rate constraints (e.g. Lexware's 0/7/19 bands), reject unmappable invoices into a `needs_mapping` status with a clear `needsMappingReason` field rather than silently dropping them — Finance needs visibility. Confidence: 0.85
- Bulk historical sync operations must throttle to match the third-party API's rate limit (e.g. Lexware's 2 req/sec) with a progress bar in the UI; surface the throttle rate explicitly in user-facing copy. Confidence: 0.82
- Account-mapping config dialogs for integrations use a Tabs layout with one tab per transaction type (Booking Revenue / Upsell / Costs / City Tax) — matches the existing Bexio/Jurnal pattern. Confidence: 0.78
- Integration-specific sync state uses a per-integration flag (e.g. `syncedToLexware` on `ReservationEntry`) independent of the shared `synced` flag used by Jurnal/Bexio — pushing to one integration must never mark a record synced for another. Confidence: 0.7
- Sync/push actions for an integration belong in the main revenue/reservation list view (alongside existing sync buttons), not only inside the integration card. Confidence: 0.6
- Invoice line items should be derived from real underlying pricing (nightlyRate × nights + cleaning fee + platform fee) reconciled to the booking total, with a fallback to a single accommodation line when the breakdown doesn't match — rather than posting a flat total. Confidence: 0.65
- Simulated provider actions (e.g. "Simulate finalize") must only claim success when the action actually applied — if the state transition is a no-op (e.g. invoice not in `draft_created`), the success toast should be guarded, not unconditional. Show an error toast ("Only draft invoices can be finalized.") instead of a misleading success. Confidence: 0.75
- Don't expose mock/simulate actions as UI buttons in integration cards (e.g. remove "Simulate finalize" entirely) — document the simulated provider webhook flow in the PRD instead, and keep the underlying state-transition function in the composable for tests. The dashboard UI should only surface real user actions. Confidence: 0.85
- Cost entries must carry a human-readable `name` field on the data model (not just type/note), and the cost detail drawer must display it at the top — the user explicitly asked to "tampilin cost name" in the cost detail. Confidence: 0.6
- Currency displays must NOT use currency symbols — format as a plain amount with the currency code appended (e.g. `750 USD`, `toLocaleString('en-US')` + code), never `Intl.NumberFormat` with `style: 'currency'` which renders `# finance
- Cost accounts should use a single default line item (not split by type) since cost types are handled via tags. Confidence: 0.70
- For Booking Revenue double-entry tables: each line item must have separate Debit and Credit account columns/selectors, not a single account with a pre-assigned debit/credit indicator. Confidence: 0.65
- No charts or progress bars — use full data tables only. Confidence: 0.90
- Use toast feedback for all user actions (create, rename, delete, toggle, save, move). Confidence: 0.85
- Use confirmation dialogs for destructive actions (delete, duplicate). Confidence: 0.80
- Header currency must dynamically reflect the tenant account's currency setting (currently CHF). Confidence: 0.70
- New accounting integrations follow a fixed structural pattern: (1) `app/components/finance/data/<id>.ts` for chart-of-accounts + helpers, (2) `app/components/finance/data/<id>-invoices.ts` for invoice types + seed fixtures, (3) `app/composables/use<Id>.ts` composable with `useState`-based state, (4) `app/components/finance/<Id>Integration.vue` + `<Id>Logo.vue`, (5) `tests/composables/use<Id>.spec.ts` covering connection lifecycle + pipeline + edge cases. Then wire into `integrations.ts` registry, `IntegrationsTab` componentMap, `useIntegrationAccounts` defaults, and `useListingMappings`. Confidence: 0.88
- For integrations with VAT/tax rate constraints (e.g. Lexware's 0/7/19 bands), reject unmappable invoices into a `needs_mapping` status with a clear `needsMappingReason` field rather than silently dropping them — Finance needs visibility. Confidence: 0.85
- Bulk historical sync operations must throttle to match the third-party API's rate limit (e.g. Lexware's 2 req/sec) with a progress bar in the UI; surface the throttle rate explicitly in user-facing copy. Confidence: 0.82
- Account-mapping config dialogs for integrations use a Tabs layout with one tab per transaction type (Booking Revenue / Upsell / Costs / City Tax) — matches the existing Bexio/Jurnal pattern. Confidence: 0.78
- Integration-specific sync state uses a per-integration flag (e.g. `syncedToLexware` on `ReservationEntry`) independent of the shared `synced` flag used by Jurnal/Bexio — pushing to one integration must never mark a record synced for another. Confidence: 0.7
- Sync/push actions for an integration belong in the main revenue/reservation list view (alongside existing sync buttons), not only inside the integration card. Confidence: 0.6
- Invoice line items should be derived from real underlying pricing (nightlyRate × nights + cleaning fee + platform fee) reconciled to the booking total, with a fallback to a single accommodation line when the breakdown doesn't match — rather than posting a flat total. Confidence: 0.65
- Simulated provider actions (e.g. "Simulate finalize") must only claim success when the action actually applied — if the state transition is a no-op (e.g. invoice not in `draft_created`), the success toast should be guarded, not unconditional. Show an error toast ("Only draft invoices can be finalized.") instead of a misleading success. Confidence: 0.75
- Don't expose mock/simulate actions as UI buttons in integration cards (e.g. remove "Simulate finalize" entirely) — document the simulated provider webhook flow in the PRD instead, and keep the underlying state-transition function in the composable for tests. The dashboard UI should only surface real user actions. Confidence: 0.85
/`€` symbols. The user explicitly asked "currencynya jangan pakai symbol untuk semua". Confidence: 0.85
- Reservation detail price section must show a full breakdown distinguishing what the guest paid from what the host receives — subtotal (nights × nightly rate), cleaning fee, service fee, tax, extras, guest-paid total, per-channel commission (deducted), and the payout amount — not just a single total. The user asked to add "detail pricenya untuk guest paid dan payoutnya" to the reservation detail sheet; model it as an optional `PriceDetails` field on the reservation with a fallback to `totalPrice` for records without the breakdown. Confidence: 0.7
- Draft statements (e.g. owner statements) must remain editable right up until publish: the publish/review dialog should include an "Add cost / revenue" action for drafts that opens a small form (type toggle Cost (−) / Revenue (+), label, amount) and appends an adjustment line — expenses stored as negative amounts (reduce payout), revenue as positive (increase payout) — with the statement total recomputed immediately via the composable's update function. The added adjustment lines themselves must also be editable and deletable before publish ("harusnya bisa edit dan delete adjustment"): every `category === 'adjustment'` row carries per-row pencil (edit, reusing the same add-line form prefilled with a dynamic "Edit line item" title, replaced in place) and trash (delete) actions; lines of other categories (revenue/expense/commission/tax/fee) remain read-only; and after publish the edit/delete controls disappear so the values lock (only via a next-period adjustment). Confidence: 0.7
- Money amount INPUT fields must surface the currency context, not just readouts: when the user enters an amount (e.g. the "Add line item" amount field in StatementPublishDialog), the field label reads "Amount (IDR)" (the statement's currency) and the currency code is repeated as a prefix inside the input with left padding (`pl-12`) so the typed number never overlaps it. The user asked "amount ini harusnya ada info currencynya apa" — an amount entry field with no visible currency is a defect; this complements (not replaces) the display-side rule that amounts render as code-suffixed plain numbers. Confidence: 0.7
- Generated statement/document artifacts (owner statement PDFs, contracts) are built as REAL PDF files with jsPDF — A4 mm units, a dark branded header band, entity info blocks, large payout figure, summary lines, and a per-item table — not browser `window.print()` of the HTML. The user supplied a sample statement PDF explicitly as the target format ("ini contoh pdf statementnya"), which prompted a proper PDF generator over the previous print-based approach. Pattern: a standalone utility in `app/lib/<domain>-pdf.ts` taking the domain records as input, returning a `Blob` (with a `download` option triggering a browser save), and mirroring the reference document's structure section by section. Confidence: 0.6
- Reservation room pricing must offer a per-night vs per-stay (flat) choice: `per_night` = nightly rate × nights, `per_stay` = flat price for the whole stay (unaffected by night count). Persist the mode on the room-line data model (`priceMode` enum + `pricePerStay`) so edit/detail views stay consistent, and when the user switches modes convert the entered value (perNight × nights → flat) instead of resetting it; date changes keep flat stays intact while recomputing per-night totals. Confidence: 0.7
- Reservation taxes & fees should be modeled as a list of named charges (each `kind` + `label` + `amount`), not a single fee field: preset kinds (Cleaning Fee, City Tax, Service Fee) plus a free-form "Other" option that reveals an inline custom-label input, each added with an amount and individually removable. The booking total rolls up as a grand total = rooms + charges + payment fee, presented in a summary breakdown (Rooms total → each charge → payment charge → Grand total), persisted on the record (`charges: ReservationCharge[]`), carried into the payment link, and rendered in the detail sheet. The user's terse directive ("tambahin fungsi untuk add tax and fee, misal citytax dan cleaning fee" — "add a function to add tax and fee, e.g. city tax and cleaning fee") means: the named examples become preset options and a custom option covers the rest, applied to both the New and Edit reservation flows without clarifying questions. Confidence: 0.65
- Reservation taxes & fees are LINKED to the selected property: when a property is picked, its configured fees should auto-populate the charges list (enabled fees from unit-type pricing, or `cleaningFee`/`serviceFee` for listings without unit types), dedupe by name, and clear property-derived charges when the property changes — while the manual add-charge form stays available so the user can still create charges beyond what the property defines ("taxes & fees itu linked sama propertynya jadi ada datanya pas pilih propert, tapi bisa buat baru juga"). Keep both paths (auto-import from property + manual add) rather than replacing one with the other. Confidence: 0.7
- Property-level Fees & Taxes are configured at the PROPERTY level (not per room/unit type) as their own settings block in the pricing tab, deliberately separate from per-unit-type (room-type) fees. The item model must follow the third-party provider's real Tax entity (Channex): `title`, `type` (`tax`/`fee`/`city_tax`), `logic` (percent/per_room/per_room_per_night/per_person/per_person_per_night/per_night/per_booking), `rate`, `currency`, `isInclusive`, `skipNights`, `maxNights`, `applicableDateRanges` — the user pasted the Channex Taxes/Tax Sets API docs to correct an over-simplified `kind`/`mode`/`value` model. Create/edit happens via a Sheet. Confidence: 0.7
- For pricing/tax config editors (e.g. the Tax Set sheet), include a live calculation preview at the bottom of the form so the user can verify how the configured rules actually apply to a price ("kalau di apply di price gimana perhitungannya"): sample inputs (base price/night, nights), then a per-rule breakdown rendered in calculation order (highest level first) with each rule's formula detail (e.g. "10% of $300", "$50 per room per night × 3 nights"), a running total, and a final grand total. Confidence: 0.65
- Fees & taxes should live at the ACCOUNT level as a single source of truth and be bulk-assigned to many listings at once, rather than configured one-per-listing — the user explicitly asked to move the property-level fees & taxes up to account level so they can "assign ke banyak listing sekaligus biar ga buat 1 per 1 listing" (assign to many listings at once so as not to create one per listing). Preferred shape: a master store composable (`useFeesTaxes`) holding the items + tax sets + a per-listing assignment id map, with the listing pricing tab reading/writing through that store so edits sync across every assigned listing (two-way sync), and a listing-level "remove" only unassigns rather than deleting the shared item. Confidence: 0.8
- When an account-level library of assignable items exists (e.g. fees & taxes), the contextual detail surface (listing detail pricing tab) should offer an "Add from library" picker to apply INDIVIDUAL items directly — a searchable dialog listing library items not already applied to that context (directly or via a bundle), click-to-apply, with an empty state pointing to Settings — complementing bundle toggles rather than forcing a trip to Settings. The user requested this with a terse directive ("bisa add fee and taxes dari library dari listing detail"). Confidence: 0.6
- A listing can have only ONE active bundle/tax set at a time: enabling a bundle replaces the currently active one (single-select), and disabling it clears the selection — the user's requirement "cuma bisa turn on 1 bundle kalo ada lebih dari 1 bundle di detail listing". Confidence: 0.8
- Rate plan Rate Mode supports only two options — Manual and Derived. Auto and Cascade are NOT valid rate modes and must be excluded from the Rate Mode dropdown in both the Add Rate Plan and Edit Rate Plan sheets (the user's directive "di rate plan itu rate modenya cuma manual sama derived"). Confidence: 0.8
