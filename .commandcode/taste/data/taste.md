# data
- Prefer real data from Elev8 Suite OS MCP over mock data when available. Confidence: 0.70
- When a new feature needs mock data, prefer reusing/enriching existing dataset records (e.g. blending rich inbox records with the larger finance table, or seeding a new bookingNote field onto an existing seeded reservation like Emily's res-3) over authoring a large fresh dataset — the user chose the blended dataset option for the reservations feature, and the booking-note addition reused the existing record. Confidence: 0.65
- Property hierarchy must support three levels: Property → Unit Type (e.g., Kingbed, Single Bed) → Unit (room with specific bed). Confidence: 0.75
- Reservation bookings that contain multiple people should be modeled as per-guest occupant records — `GuestOccupant` (name, category adult/child/infant, email/phone, DOB, nationality, passport/ID-card type + number, isPrimary flag) — not just a numeric guestCount. Asked which fields a guest group needs, the user chose the "Lengkap + identitas" (full identity details) option. The UI mirrors the model: a party breakdown ("2 Adults · 1 Child · 1 Infant") + overlapping avatar stack (max 4 + "+N") in the Booking Info card, and a per-occupant list (avatar, Main badge, category, DOB, nationality, contact, ID number) in an accordion in the reservation detail sheet. In the detail sheet's guest block the user explicitly asked to show the adult/child/infant split instead of a bare "N guests" line ("munculin info adult berapa child infant berapa") — the breakdown is derived from the occupant records' categories and falls back to the total guestCount only when no occupant data exists. Confidence: 0.72
- Keep aggregate reservation counts consistent with the occupant records when seeding/enriching mock data: adding an occupant (e.g. a child to Emily's booking) must bump `guestCount` to match, so the party breakdown, the count, and the per-guest list never contradict each other. Confidence: 0.5
- When one record of a type is the canonical/reference example (e.g. Emily Chen's res-3 carrying full `priceDetails`, per-guest occupant records, and a `bookingNote`), the user expects ALL sibling records of that type to carry the same rich structure so detail views render uniformly — flat records silently hide UI sections (price breakdown, Guests accordion, booking note in the reservation detail sheet). Enrich every sibling to match the reference and keep `guestCount` consistent with the occupant arrays; leave flat only records that legitimately aren't that type (e.g. a manual calendar block, not a real booking). When replicating the reference's text fields (bookingNote), mirror the format verbatim but adapt values per record — currency/amounts must match that reservation's own totals and channel (OTA bookings say "Payment Collect: OTA collect", a direct booking says "Direct Payment"). User directives: "update struktur detail reservasi di reservasi lain seperti emily chen" and "cek data yang ada di emily chen tapi gaada di reservasi lain kaya booking notes ... terus info guest paid sama payout". Activity timelines follow the same rule: one canonical reservation (Emily) carries a full `activity` timeline and the user expects every sibling to have one too — seed chronological events (confirmed, payment, guide sent, cleaning, check-in/out, guest messages) whose content matches each reservation's own status/stage (an inquiry stays short, ending before check-in; a checked-out stay runs through check-out) and whose channel reflects the booking's channel (WhatsApp/email/OTA). Identity-verification statuses were seeded the same way: every sibling reservation got an `identity` record whose status matches its own stage — checked-out stays verified, submitted-but-unreviewed stays pending, inquiries not_started with no documents. Confidence: 0.8
- When seeding mock documents/media across many records, generate them via a small helper (e.g. `seedIdentity()` building consistent SVG data-URI placeholders) rather than hand-writing long inline literals per record — the assistant switched from verbose hand-written data URIs to the helper explicitly to stay concise ("Data URI manual panjang sekali... jauh lebih ringkas"). Confidence: 0.6
- Seed/demo data must obey the same business-rule validations the app enforces ("seed datanya juga gaboleh melebihi batas quota per year" — seed data must not exceed the annual cap). When adding a new validation that rejects values (e.g. a seasonal quota window's maxNights over the owner's annual use cap), audit and update existing seed records to comply (an Apr–Jun window of 20 nights was lowered to 14 = the annual cap), and update dependent tests (remaining nights 17 → 11) so no seed record sits in a state the UI/validation would now reject. Confidence: 0.7
- Seeded records that participate in a runtime flow must carry the same relation links as records created through that flow — a contract seeded directly with status `'sent'` needs a `documentId` into the Document Center exactly like one produced by `generateContract`, otherwise the feature silently breaks only for seeded records (owner signs the contract but it never appears in their Documents page, because only the generate flow creates the document). Fix in both directions: align the seed, and make the consuming action self-healing (e.g. `signContract` ensures the Document Center entry exists when `documentId` is missing) so pre-existing/legacy records recover without a data migration. Confidence: 0.6
- Guest counts should be split into three separate numeric fields — Adults, Children, Infants — rather than a single `guestCount` input. When the user asked "untuk field guests itu dibagi jadi 3 yaitu adult, children, infant", the expected implementation was three number inputs (3-column grid with a computed "N total" caption), the reservation types extended with optional `guestAdults`/`guestChildren`/`guestInfants` fields (on both draft and saved records), `guestCount` derived/synced as their sum via a watcher, and seed data enriched to carry the breakdown. This is the input/model counterpart to the existing adult/child/infant party-breakdown display convention. Confidence: 0.75
- When the same real-world entity is referenced across modules in divergent ID spaces (e.g. Journey scope keyed by listing NAME from a revenue-derived ~86-name list, while WhatsApp assignment used `lst-*` `listingIds` from a 24-listing canonical set), prefer standardizing both sides to ONE canonical dataset with stable IDs so scope and assignment share a key — the user chose the "Canonical listings.ts (Recommended)" option over name-matching or partial-coverage workarounds when asked how the coverage check should resolve scope. Confidence: 0.7
- When the user reveals that a term or acronym used for a feature maps to an EXISTING role/entity (e.g. "GRO itu dah guest experience manager" — GRO is just the existing "Guest Experience Manager" role, not a new one), do not create a redundant parallel role/entity: reuse the existing one, fold any genuinely-new attribute into it (e.g. cross-tenant `assignedTenantIds` moved onto the existing user-1), remove the redundant role and any redundant seed user, re-point every consumer (`isGro` role check, `UserDetailSheet` tenant picker, notification policies) to the existing entity, and grep for leftover references to confirm a full cleanup. Confidence: 0.7
- When a reference screenshot/UI introduces a fixed taxonomy (e.g. action-needed categories + priorities) that isn't present in the data model, add explicit typed enum fields to the model (a `ActionCategory` union type + `ActionPriority` union) and seed them onto the relevant records — rather than heuristically deriving categories from existing labels/free-text content. The user chose the "add field" option (over "derive from existing data" / "category only") when asked where the taxonomy should come from; derivation from `labels`/`lastMessage` was explored and rejected as unreliable because the data had no field matching the reference's categories. Confidence: 0.6
- Fees and taxes are configured at the PROPERTY level, not per unit type/room: when the user noticed a duplicate Fees section in the per-unit-type pricing accordion, they asked to delete it because it already existed at the property level ("hapus fees section yang di room type karena itu udah di propert type yang fees & taxes"). Avoid duplicating the same setting across the property → unit type → unit hierarchy — when a setting lives at one level, remove the redundant copy at a more granular level so there is a single source of truth. Confidence: 0.7
- When two closely-related entity types are managed with identical fields and semantics (fees and taxes), model them as ONE collection with a discriminator/type field rather than separate parallel arrays — the user asked "fees and taxes jadi 1" (fees and taxes become one), which replaced the separate `fees`/`taxes` arrays with a single `feesTaxes: ListingFeeTaxItem[]` where each item carries `kind: 'fee' | 'tax'`. The UI mirrors this: one "Fees & Taxes" card with a single "Add fee or tax" action, a Type (Fee/Tax) selector inside the create form, and a unified list rendering each item's kind as a badge — instead of two parallel sections. Confidence: 0.7
- For account-level/settings configuration that should apply broadly (e.g. cancellation policy), prefer a SINGLE global default + per-entity override model over a "library + multi-assign" model: one default config applies to every entity, and individual entities can carry an override stored keyed by entity id (`overrides: Record<id, config>`), with the effective value resolved as `override ?? default` (helper like `effectiveConfigForListing` / `hasOverride` / `setOverride` / `clearOverride`). The user explicitly redirected the cancellation-policy feature from "create a library of policies and assign them to listings" to "1 settingan default untuk semua listing tapi bisa di override di tiap listing" (one default setting for all listings, overridable per listing); the per-rate-plan `inheritCancellationPolicy` flag was later restored as a THIRD override level — the cancellation-policy override model is now three levels: account default → listing override → rate-plan override (stored inline on the rate plan as `inheritCancellationPolicy: false` + its own `cancellationPolicyConfig`). The Settings panel must surface ALL override levels, including a read-only "Rate Plan Overrides" list, so a partial override of just one rate plan/room type in a listing is visible rather than silently hidden (the user surfaced this exact edge case: "kalau di 1 listing cuma 1 roomtype rates aja yang di override gimana?"). Confidence: 0.8
- In the listing detail (pricing tab), the cancellation policy lives at the RATE PLAN level — configured per rate plan inside the Add/Edit Rate Plan sheet via `CancellationPolicyEditor` — not as a property/listing-level card or override sheet. When the user saw a listing-level "Cancellation Policy" card they corrected the placement ("cancellation policy yang di listing detail di level rates settingnya bukan di property"), so the listing-level card/sheet was removed and the rate-plan card reverted to `cancellationPolicySummary(rp.cancellationPolicyConfig)`. This is a distinct surface from the account-level Settings page, whose single global default + per-listing override model stays. Confidence: 0.75
