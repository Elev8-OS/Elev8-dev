# documentation
- Include a concise executive summary / quick-reference section at the top of PRD documents so developers can understand the feature at a glance before diving into details. Confidence: 0.70
- Write PRDs and technical specs as implementation-ready documents without labeling anything as "mock" — developers will build from them as real specifications. Confidence: 0.70
- PRDs should be short but detailed ("singkat tapi detail") — concise enough to skim quickly, yet still covering requirements, flows, data model, business rules, and acceptance criteria. Confidence: 0.75
- When writing a new PRD, follow the repo's existing PRD conventions (TL;DR quickstart at top, then requirements/flows/data model/acceptance criteria) and place the file at the repo root with a `PRD-<Feature>.md` name alongside the existing PRDs (e.g. `PRD-Email-Integration.md`, `PRD-Review-Hub-v2.md`, `PRD-Owner-Portal.md`) — the repo's PRDs live at the root, not in `docs/`. Confidence: 0.7
- When making a PRD canonical/final, include an explicit "Known Gaps" section documenting not-yet-implemented items with their risks — the PRD becomes the single source of truth for what is implemented versus what still needs work. Confidence: 0.7
- PRDs should include an explicit Scope section split into "In Scope" vs "Out of Scope", with out-of-scope items cross-referenced to the Known Gaps section numbers so scope boundaries stay traceable. Confidence: 0.8
- New feature design specs are committed to `docs/superpowers/specs/` following the repo's existing date-prefixed naming convention (e.g. `2026-08-07-reservations-guest-profiles-design.md`), committed with a conventional `docs:` message, then handed to the user for review before implementation planning begins — the user approved the "write the design doc and commit it" step as part of the design workflow — a bare "ya" approving a presented design that ends with "kalau ya, saya tulis design doc-nya" triggers this whole chain (write spec to `docs/superpowers/specs/` with date-prefixed name, commit with `docs:` message, hand to user for review) without further confirmation. Confidence: 0.65
- The user expects a PRD document to accompany a shipped feature: a terse directive like "push ke github dan buatin prdnya" means write the PRD (modeled on the repo's existing PRD template), commit it together with the feature code on the feature branch, and link it in the PR. The PRD should record the product decisions already agreed in conversation — what is simulated vs real, optional gates (e.g. MX), and out-of-scope items that UI copy claims but code hasn't wired — so the PR is self-documenting. Confidence: 0.7
bles, data, tests) via an explore agent, read the existing design docs and at least one existing root-level PRD to match conventions, verify what is actually shipped in code (e.g. the split Occupancy/ADR chart layout) rather than trusting docs alone, then write the PRD at the repo root and report. The PRD covers both the tenant (staff) and external surfaces of the feature, and records shipped invariants plus Known Gaps. Confidence: 0.7
- When the user asks to "buat requirementnya ... yang detail termasuk DoD" (write the requirements, detailed, including the DoD), include an explicit **Definition of Done (DoD)** section in the PRD, organized by category (Functional / UI / Journeys / Persistence / Quality) with testable checkbox items — as a separate section from the Acceptance Criteria list, not merged into it. Confidence: 0.7
- When the user states a requirement must be real for developers to implement ("this requirement not for mock, real requirement for dev to implement") and names the bridge/vendor API to use (e.g. Minut via the HostBuddy API), write the spec as a real implementation-ready document — real server endpoints, server-side persistence, token/webhook security — replacing earlier mock internals while keeping the same UI surface and public composable API. Don't describe the feature as a client-side mock. Confidence: 0.7
- When a real integration's provider/bridge API contract is unavailable (not in the codebase and no public docs), do not invent endpoint paths, auth models, or payload schemas — describe what Elev8 needs (its own operations/endpoints, the webhook contract shape) and record the provider's exact endpoint/auth/schema as an explicit "Open Questions" section for engineering to resolve. The user defers API-contract specifics to the developer ("need to check from dev"). Confidence: 0.7
- When the user revises a requirement's scope with a terse English "for now is no X" (e.g. "for now is no email"), treat it as a phase-deferral directive — remove X from the current scope without asking clarifying questions and propagate the removal consistently across the entire PRD (TL;DR, problem statement, feature summary, scope, data model, acceptance criteria), then grep the doc for leftover references to confirm none remain. "for now" marks a temporary deferral rather than a permanent exclusion, so the scoped-out channel/capability may be reintroduced in a later phase. Confidence: 0.55
- When asked to update an existing PRD to match shipped behavior (terse Indonesian "tolong update di requirementnya"), read the current PRD first and rewrite every affected section to reflect the ACTUAL implemented behavior rather than the original aspirational spec — update the feature-summary table, correct stale user-flow/system-logic bullets (e.g. "warning triangle when disconnected" → "no triangle when connected"), add new behavior subsections (e.g. "Not-connected behavior"), expand + renumber the acceptance criteria, add data-model helper signatures, and update Known Gaps + Related Modules — then report a section-by-section summary of what changed. The directive arrives in terse Indonesian ("tolong update di requirementnya") or English ("update the requirement"). The PRD is expected to remain the single source of truth matching shipped code. Confidence: 0.7
- After changing a data model or API field format in code (e.g. rate plans reworked to the Channex schema), the user asks to verify the docs still match with a terse Indonesian check directive ("cek dokumentasi apakah sudah sesuai settingan rate plannya?") — treat it as a doc-sync verification pass: grep the docs for both the new terms AND the removed old ones (e.g. `pricePerNight`/`pricePerAdditionalGuest`) to find every affected spec/PRD section, then confirm whether each matches the shipped settings and update stale references so docs stay aligned with code. The pass must also update the agent-context files — AGENTS.md and CLAUDE.md both carried the stale `RatePlan` shape (`pricePerNight`/`pricePerAdditionalGuest`) and were rewritten to the new Channex-style structure (with helper names `createRatePlan()`/`ratePlanNightlyRate()`/`ratePlanMaxOccupancy()`), not just specs/PRDs. Before rewriting a stale-looking field reference, verify which entity it belongs to: `pricePerNight` in the multi-room-booking spec is a `ReservationRoomLine` (separate reservation type) field and stays valid, and references like `rp.isBase` / `RATE_PLAN_UNMAPPED` still hold — only genuinely-stale references to the reworked entity are changed. Confidence: 0.6
