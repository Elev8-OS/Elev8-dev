# vue-nuxt
- Reka UI SwitchRoot ignores external `:checked` prop changes — use `:key` force re-mount as workaround. Confidence: 0.85
- Reka UI CheckboxRoot `:key` force-remount workaround doesn't fix visual state — replace with plain `<span>` + Tailwind classes instead. Confidence: 0.85
- For checkbox multi-selects and toggle check controls, use the shadcn-vue/Reka UI `<Checkbox>` component (auto-imported globally, no import statement needed) rather than hand-rolled `<button>` toggle controls — the user explicitly said "checkbox pakai komponen reka ui" when a custom button-based check/check-square toggle for the commission-basis deductions was built. Accepted pattern: a `<label class="flex items-center gap-2 ...">` row wrapping `<Checkbox :model-value="...includes(key)" @update:model-value="toggle(key)" />` + the label `<span>`. CRITICAL binding detail: Reka `CheckboxRoot` uses `modelValue` + emits `update:modelValue` — NOT `:checked`/`@update:checked`. Binding with `:checked`/`@update:checked` (which some in-repo examples appear to use and which AGENTS.md flags as a known bug) never connects to state, so nothing changes when clicked (user: "ngga berubah infonya pas check checkboxnya" — the summary info didn't update when checking a checkbox); the fix was switching to `:model-value` + `@update:model-value`. Verify a Reka control's actual prop/emit (read `node_modules/reka-ui/dist/index.d.ts`, e.g. CheckboxRoot around the props/emits declarations) before assuming a binding works, and when state doesn't react to a control, suspect a wrong v-model binding name first. Use the project's existing Checkbox usage (grep `<Checkbox`/`ui/checkbox`) as the canonical form before inventing a new toggle control. Confidence: 0.85
- shadcn-vue Input/Textarea require `:model-value` + `@update:model-value` bindings, NOT `:value` + `@input`. Confidence: 0.85
- Nuxt auto-imports components with directory prefix: `components/inventory/FooDrawer.vue` → `<InventoryFooDrawer>`. Confidence: 0.80
- "Duplicated imports" warnings on build arise when two composables re-export the same types (e.g. `useOwners.ts` and `useOwnerPermissions.ts` both re-exporting `OwnerPermissionConfig`/`OwnerPermissionTemplateId`/`OwnerStatementField` from the same data file): Nuxt auto-import merges both into one namespace, then Rollup ignores one (non-fatal since identical). Before cleaning such re-exports, verify no consumer imports those types from both sources (grep for `import ... from '~/composables/useX'` and `import type` usages); components getting the types via `~/components/.../data` directly are unaffected. Confidence: 0.6
- When a component placed in a path-prefixed subfolder is referenced under its short name (e.g. `components/owner-portal/OwnerSignaturePad.vue` used as `<OwnerSignaturePad>` → the auto-import name is actually `OwnerPortalOwnerSignaturePad`), the control silently renders nothing and the dev log shows `Failed to resolve component` (user report: "engga muncul drawnya" — the signature drawing never appeared). In that instance an explicit import did NOT reliably clear the warning (Nuxt's globally-registered auto-import name can override the local import); the robust fix was moving the file to the `components/` root so the auto-import name matches the usage. When a just-shipped control "doesn't show up", check the dev log for `Failed to resolve component` first. Confidence: 0.7
- After moving a Nuxt component file to fix auto-import resolution (e.g. `components/owner-portal/OwnerSignaturePad.vue` → `components/` root + explicit import update), HMR alone is NOT enough — the dev log kept resolving the OLD path (`Vue Router warn: No match found for location with path "/_nuxt/components/owner-portal/OwnerSignaturePad.vue"`) and the signature pad still failed to render in the browser until the dev server was killed and restarted fresh so Nuxt re-scans its component manifest. Verify the fix on the fresh server: curl HTTP 200 + a clean dev log (no `Failed to resolve component`, no old-path router warnings). Don't grep SSR HTML for content gated behind client-side state (e.g. the contract e-sign page renders nothing server-side until a pending email exists — canvas/Signature markup only appear after hydration + magic-link state). If the user still reports nothing on their side after a clean server, point them to a hard refresh (Cmd+Shift+R) to drop the cached bundle. Confidence: 0.7
- When a component placed in a path-prefixed subfolder is referenced under its short name (e.g. `components/owner-portal/OwnerSignaturePad.vue` used as `<OwnerSignaturePad>` → the auto-import name is actually `OwnerPortalOwnerSignaturePad`), the control silently renders nothing and the dev log shows `Failed to resolve component` (user report: "engga muncul drawnya" — the signature drawing never appeared). In that instance an explicit import did NOT reliably clear the warning (Nuxt's globally-registered auto-import name can override the local import); the robust fix was moving the file to the `components/` root so the auto-import name matches the usage. When a just-shipped control "doesn't show up", check the dev log for `Failed to resolve component` first. Confidence: 0.7
- Use `useState<T>()` with spread syntax for shared reactive state in composables. Confidence: 0.80
- Consolidate multiple imports from the same module onto one line: `import { createEmptyTemplate, type WhatsAppTemplate } from '~/components/.../whatsapp-templates'` rather than separate lines for value and type imports. Confidence: 0.80
- When a shared form/select component is used by multiple consumers with different value semantics, keep it backward-compatible by adding an OPTIONAL id/stable-value field to the option type (e.g. `PropertyPickerOption.id?: string`) and falling back to the existing value (name) when absent — the user picked this "canonical listings.ts" approach to standardize Journey scope to listing IDs without breaking name-only consumers. Confidence: 0.6
- Perfectionist import-order rule in this codebase: internal `import type` (from `~/`) first, then external package imports (e.g. `vue-sonner`), then internal `~/` value imports, then relative imports — placing an external import before the type imports or among the `~/` value imports fails lint (hit again when adding editable status: the mis-ordered `vue-sonner` import failed lint until `eslint --fix` sorted it). Within a group imports are sorted alphabetically by path (e.g. `SendEmailAction` before `SendGuestGuideAction`; UI component imports alphabetically by `~/components/ui/<name>`), and a mis-ordered new import in the middle of an import block is a lint failure the assistant must fix before merge. Confidence: 0.82
- When a typed union of string literals (e.g. `sellMode: 'per_room' | 'per_person'`, `rateMode: 'manual' | 'derived' | 'auto' | 'cascade'`) is used in object literals that are merged into one array across a `.map()` spread, TypeScript widens the literal to plain `string` and the array no longer matches the `RatePlan`-shaped prop — the fix is explicit `as const` on each literal property (`sellMode: 'per_room' as const`) or a full object type annotation, so the union discriminant survives the array construction. Confidence: 0.55
- Place all `<script setup>` imports at the top of the block — never interleave `import` statements with reactive state declarations or function definitions. Confidence: 0.85
- Use `withDefaults(defineProps<{ compact?: boolean }>(), { compact: false })` pattern for optional props (any type) that need a default — ESLint `vue/no-required-prop-with-default` requires the prop be marked optional whenever a default is provided. Confidence: 0.78
- Nuxt auto-import does NOT resolve sibling components nested in a subdirectory (e.g. `components/reservations/`) under their short names when used by other components in the same folder — `<ReservationGuestCell>` inside `ReservationTable.vue` and `<EditReservationDialog>` inside `ReservationDetailSheet.vue` both produced a runtime `Failed to resolve component` warning even though typecheck passed. Add explicit `import ... from '~/components/reservations/X.vue'` for such siblings. A control wired to an unresolved component appears unresponsive (user: "gabisa klik edit"), so check for missing imports first when a button seemingly doesn't work. Re-confirmed when `components/owners/StatementTable.vue` failed to resolve for its sibling `OwnerStatementsPanel.vue` and an explicit `~/components/owners/StatementTable.vue` import cleared it (user: "bug ga muncul"); the dev log showed the warning exactly twice (pre-fix renders) and zero times after the explicit import — the warning count is a precise signal that the fix landed (HMR'd files also re-render cleanly). Confidence: 0.95
- When props are only used in the template, declare `defineProps<{...}>()` without assigning it to a variable: `const props = defineProps(...)` trips the unused-vars lint rule, but removing the call entirely breaks template type inference — the bare call is the clean middle ground. Confidence: 0.75
- When a shadcn-vue `Select`'s `@update:model-value` handler signature conflicts with `AcceptableValue`, either cast the callback inline (`@update:model-value="(v: any) => ..."`) or widen the declared handler's parameter to `unknown` and cast inside the function — both pass typecheck + lint (widening to a specific type like `string` still fails; the editable-status handler was fixed by widening to `unknown`). Confidence: 0.8
- Place `defineProps` before `defineEmits` in `<script setup>` blocks (ESLint enforces this macro ordering — props first, then emits, both right after imports). Confidence: 0.85
- Remove unused helper functions before committing; ESLint `no-unused-vars` will fail CI lint and block PR merge if a declared function is left without a call site. Confidence: 0.82
- When seed/fixture data needs to represent an edge case that violates the production type (e.g. a non-conforming VAT rate for a `needs_mapping` invoice), widen the production type to `number` rather than casting at the seed site — keeps the type honest about what the model can carry while still validating the constraint at the pipeline boundary. Confidence: 0.80
- Reka UI Calendar `v-model` bound to a transforming computed (getter/setter that converts a `CalendarDate` to a string) does not reliably commit a date selection, especially for a Popover nested inside a Sheet; instead bind a plain `ref<CalendarDate | null>` with `:model-value` + `@update:model-value="commitDate"` (emit `val.toString()`) and a `watch` syncing back from the string prop. The same computed-based pattern works fine in a Dialog context, so verify in the actual Sheet context. Confidence: 0.65
- When the same composable is called in multiple components and its state must be shared (e.g. analytics filters consumed by both a filter bar and the KPI/chart components on one page; review-hub sort consumed by both the page and `FeedTable`), back the refs with `useState(sharedKey, ...)` instead of plain `ref` — plain refs create a fresh instance per call site, so a control toggled in one component silently diverges everywhere else (user: "ngga berfungsi sortnya" — the sort header toggled its own state but `filteredFeedItems` was computed from the page's separate instance). Fix by converting the refs to `useState` with shared keys, matching the repo's existing pattern. Confidence: 0.85
- When a detail panel (sheet/drawer) receives an entity as a prop but that entity is editable through a shared composable that REPLACES objects in its array (e.g. `updateReservationStatus` maps a new object into `reservations`), don't render from the stale prop object — resolve the entity from the composable's live state via a computed lookup by id with fallback to the prop (e.g. `reservations.value.find(r => r.id === props.reservation!.id) ?? props.reservation`) so edits reflect immediately in the detail UI. The user reported "waktu change status, muncul info tapi statusnya ngga berubah" (toast confirmed but badge/dot/dropdown stayed stale) because the sheet held the old object reference while the table updated. Confidence: 0.68
- When a detail panel (sheet/drawer) receives an entity as a prop but that entity is editable through a shared composable that REPLACES objects in its array (e.g. `updateReservationStatus` maps a new object into `reservations`), don't render from the stale prop object — resolve the entity from the composable's live state via a computed lookup by id with fallback to the prop (e.g. `reservations.value.find(r => r.id === props.reservation!.id) ?? props.reservation`) so edits reflect immediately in the detail UI. The user reported "waktu change status, muncul info tapi statusnya ngga berubah" (toast confirmed but badge/dot/dropdown stayed stale) because the sheet held the old object reference while the table updated. Confidence: 0.68
- Unovis chart components are unreliable/ill-suited for custom visualizations in this codebase: `VisScatter` drops points via its domain calculation (x/y-domains don't reliably apply), and `VisGroupedBar` is too cramped for many rows with long labels (e.g. 18 category tags). For simple custom visualizations (bubble chart, grouped horizontal bars per category), implement with plain HTML/CSS (flex rows + proportional-width bars) rather than wrapping the library — renders reliably and keeps labels readable. Confidence: 0.68
- Unovis LineChart legend items are built once at mount and do NOT react to later `legendLabels` prop changes — when legend labels derive from reactive state (e.g. a comparison-mode switch), add a `watch` on the labels prop that rebuilds the legend item names, and key the line-opacity lookup by series index rather than by (possibly renamed) item name. Confidence: 0.7
- Unovis ChartCrosshair tooltip data: when series use renamed/custom legend labels (names no longer equal the raw data keys), match legend items by index/position rather than by `name === key` lookup (name lookup returns undefined once labels are renamed), and pass the chart's `categories` prop through to restrict tooltip rows to only that chart's series — otherwise `omit(d, [index])` leaks every other series stored in the same datum object into the tooltip. Confidence: 0.75
- When TS strictness flags a possibly-undefined result of string indexing (e.g. `new Date().toISOString().split('T')[0]`), fix with a `?? ''` fallback rather than casting or restructuring — this matches the repo's established fix pattern (the `createReservation` composable fix uses the same idiom). Confidence: 0.55
- Reka UI ScrollArea inside a flex-column SheetContent does NOT scroll with `flex-1` alone — the ScrollAreaViewport needs a bounded height to scroll. The canonical fix: `h-full min-h-0` on the ScrollArea (allows the flex child to shrink below content height) plus `overflow-hidden` on the SheetContent. The user reported "i cannot scroll on the sheet" and verified the fix after it was applied. Confidence: 0.8
- Reka UI (shadcn-vue) `SelectValue` with a custom `#default` slot containing a component (e.g. a `ReservationStatusBadge`) does NOT render the selected value in the trigger — the status badge appeared missing from the reservation detail sheet. Fix: put the custom content (badge) directly inside `SelectTrigger` (always visible) and keep `Select`/`SelectContent` for the dropdown; `SelectTrigger` already lays out slot content + chevron (`flex items-center justify-between gap-2`), and the trigger's `*:data-[slot=select-value]` styling selectors only apply to an actual SelectValue, not to the badge. Confidence: 0.65
- shadcn-vue `Tabs` render content lazily/client-only, and pages wrapped in `<ClientOnly>` ship only a skeleton in SSR — so grepping the rendered SSR HTML for tab-content markers (e.g. "Stay Requests", "Create owner" tabs added to a page) returns nothing even though the feature works after hydration. For such client-only surfaces, HTTP 200 + a clean dev log is the reliable smoke signal; verify actual tab content via component tests or the client bundle instead of SSR HTML greps. Confidence: 0.62
- The antfu ESLint config enforces `antfu/if-newline`: a single-line `if (cond) return` (statement on the same line as the condition) fails lint — break the statement onto the next line (`if (cond)\n  return`). Hit repeatedly when adding guard clauses to new handlers (e.g. `handleTranslate` in ReplyPanel and HostReviewPanel); the rule also flags long-standing pre-existing code in the same files, so fix it only in newly added code and leave pre-existing violations alone. Confidence: 0.6
- When a third-party library exports a lowercase class constructor (e.g. `jsPDF` from `jspdf`), the antfu ESLint `new-cap` rule rejects `new jsPDF()` — fix by aliasing the import to PascalCase (`import { jsPDF as JsPdf } from 'jspdf'` then `new JsPdf({ unit: 'mm', format: 'a4' })`) rather than adding an eslint-disable comment or casting. Confidence: 0.6
- Nuxt `.global.ts` route middleware applies to EVERY matching path, so a page that must be reachable WITHOUT a session (e.g. the owner contract e-sign page, where the owner lands via a magic link and signs before a session exists) needs an explicit whitelist entry in the middleware — otherwise the guard 302-redirects to login and the user reports the flow as broken ("gabisa di klik open demo secure link" — the click went nowhere because the target route bounced back to login). Whitelist such pre-auth pages in the middleware itself; the page can still guard its own content (e.g. only render the contract when a pending email is present). Confidence: 0.6
- A template `ref` bound to a shadcn-vue/Reka UI component (e.g. `TabsList`) resolves to the component INSTANCE, not the underlying DOM element — reading DOM layout properties from it (e.g. `scrollWidth`/`scrollLeft` for dynamic scroll-fade indicators) silently returns nothing, so the feature never activates (user: "masih bug ngga muncul fadenya" — the fade still didn't show). Fix: make a real wrapper `<div>` the scroll container and put the ref there, and give the inner component `w-max min-w-full` so its content can exceed the container width and actually trigger overflow/scroll. Confidence: 0.8
- DOM-measurement/layout logic inside a sheet/drawer whose body is lazily rendered (content gated by `v-if` on the entity prop) must NOT live only in `onMounted` — by the time the panel first opens, the mount hook has already fired and the ref is null, so the measurement never runs (scroll fades never appeared on the owner detail sheet). Add a `watch` on the open prop that re-runs the measurement in `nextTick` each time the panel opens, in addition to the mount-time call and the scroll/resize listeners. Confidence: 0.75
- A template `ref` bound to a shadcn-vue/Reka UI component (e.g. `TabsList`) resolves to the component INSTANCE, not the underlying DOM element — reading DOM layout properties from it (e.g. `scrollWidth`/`scrollLeft` for dynamic scroll-fade indicators) silently returns nothing, so the feature never activates (user: "masih bug ngga muncul fadenya" — the fade still didn't show). Fix: make a real wrapper `<div>` the scroll container and put the ref there, and give the inner component `w-max min-w-full` so its content can exceed the container width and actually trigger overflow/scroll. Confidence: 0.8
- DOM-measurement/layout logic inside a sheet/drawer whose body is lazily rendered (content gated by `v-if` on the entity prop) must NOT live only in `onMounted` — by the time the panel first opens, the mount hook has already fired and the ref is null, so the measurement never runs (scroll fades never appeared on the owner detail sheet). Add a `watch` on the open prop that re-runs the measurement in `nextTick` each time the panel opens, in addition to the mount-time call and the scroll/resize listeners. Confidence: 0.75
- Reka UI (shadcn-vue) `SelectValue` with a custom `#default` slot containing a component (e.g. a `ReservationStatusBadge`) does NOT render the selected value in the trigger — the status badge appeared missing from the reservation detail sheet. Fix: put the custom content (badge) directly inside `SelectTrigger` (always visible) and keep `Select`/`SelectContent` for the dropdown; `SelectTrigger` already lays out slot content + chevron (`flex items-center justify-between gap-2`), and the trigger's `*:data-[slot=select-value]` styling selectors only apply to an actual SelectValue, not to the badge. Confidence: 0.65
- reka-ui (shadcn-vue) `Pagination` is a poor fit for simple manual table pagination: `PaginationRoot` requires an `itemsPerPage` prop, each `PaginationItem` requires a numeric `value` prop, and Nuxt auto-import leaves these components' types unresolved for `vue-tsc` (they type as `{}`), so even explicit imports still fail typecheck on the required props. The accepted pattern for simple pagination is a custom Button-based pager: outline prev/next Buttons with `lucide:chevron-left/right`, numbered ghost Buttons (`min-w-8`) with the active page highlighted via `data-[active=true]:bg-primary data-[active=true]:text-primary-foreground`, an ellipsis `…` span in the page-number run when pages are far apart, a `watch` on the filters/search resetting `page` to 1, and a "Showing X–Y of Z items" footer that renders only when the list exceeds the page size — shipped for the Users table (8 per page) after the reka-ui wrapper was abandoned for type complexity. Confidence: 0.6
- TypeScript discriminated-union records stored in a `useState`-based composable array: when adding/updating a member via object spread (`{ ...input, id }` or `{ ...rule, ...patch }`), the spread flattens the union's discriminant and TS loses the exact member shape — `vue-tsc` errors on the `.value = ...` assignment. Fix by casting the spread result back with `as CommissionRule` (with a brief comment explaining the union spread) rather than adding unnecessary branches or restructuring. Applied to `addRule`/`updateRule` in `useOwners.ts` for the `CommissionRule` union. Confidence: 0.55
- reka-ui `RangeCalendar` `v-model` collides with `@internationalized/date` types: the project ends up with two `@internationalized/date` copies (reka-ui's internal `DateValue` vs the directly imported `CalendarDate`), so a `ref<DateRange>` (from reka-ui) seeded with `new CalendarDate(...)` fails typecheck, and importing `CalendarDate` from `@internationalized/date` for the constructor doesn't satisfy the reka-ui type. The pragmatic pattern (used for the owner seasonal-quota date range, matching the reservations page's loose typing): type the range ref loosely (`ref<any>` / helpers returning `any`), construct seed dates with `new CalendarDate(y, m, d)` from `@internationalized/date`, convert selections to ISO strings via `date.toDate(getLocalTimeZone()).toISOString().split('T')[0]` (guarding invalid/empty values), and keep the form's ISO start/end strings in sync with a deep `watch` on the range plus a reverse `watch` on the target input that re-seeds the picker when the dialog opens with existing values. Confidence: 0.6
- For Nuxt composable state shared across PAGES (e.g. an account-level Settings store read by both the Settings page and a listing-detail component), use a MODULE-LEVEL `ref` singleton (matching the `listings`/`payoutAccounts` pattern) rather than `useState()` — `useState` gets overwritten by the server payload on client-side navigation between pages, so edits made on one page silently reset when navigating to another (user-reported: Settings fee/tax edits didn't sync to listing detail). `useState` remains fine for same-page multi-component sharing (analytics filters, review-hub sort), but cross-page singletons need module-level `ref`. Confidence: 0.75
- `defineModel()` refs read STALE values within a single synchronous handler: after `model.value = { ...model.value, bookingMode: 'entire_property' }` is assigned, a follow-up read of `model.value` (e.g. in a helper that builds the room lines) still returns the OLD prop value because the parent hasn't re-rendered yet — so a second `model.value = ...` write clobbers the first and the mode appears to reset (user: "tab entire propertynya ngga berfungsi"). Fix: compute the ENTIRE final state first, then assign `model.value` exactly once (build the room lines into a local const, then a single `model.value = { ...model.value, bookingMode: v, rooms: ... }`). Apply the same single-write pattern to any computed setter over `defineModel`. Confidence: 0.75
- Mutating a prop object's fields in place (`props.config.policy = ...`, `props.config.refundTiers.push(...)`) fails the project's `vue/no-mutating-props` lint rule. The idiomatic shadcn-vue fix for an editable config object passed into a child editor component is `defineModel<T>('config', { required: true })` bound from the parent via `v-model:config`, with every mutation replaced by an immutable reassignment (`model.value = { ...model.value, field: next }`), so the child never mutates the prop reference in place. Confidence: 0.6
- Async submit/action handlers that toggle a loading flag must use `try/catch/finally` so the flag is reset in `finally` — otherwise any rejected promise (or throw after the record was already updated) leaves the spinner stuck on "Submitting..." forever. Pair this with wrapping side-effect helpers (e.g. notification creation) in their own try/catch as defense-in-depth so a side-effect failure can never reject the main await. Confidence: 0.7
- Composables that need Nuxt/Vue injection context (e.g. `useNotifications()`) should be invoked ONCE at the top of the composable/setup function and the returned instance reused — do NOT re-invoke `useX()` inside `.then`/async callbacks or event handlers where the active-component context may be absent: re-invoking there can throw (no setup context), reject the surrounding promise, and leave the UI stuck even after the underlying record was already updated (the guest-registration "stuck on Submitting..." bug was ultimately fixed by hoisting `useNotifications()` to the top of `useGuestRegistration()` and reusing it, rather than only try/catching the call inside the callback). Confidence: 0.6
- Nuxt composable (`.ts`) file changes do NOT reliably hot-reload via HMR (the dev log only lists `.vue` file HMR updates, never the `.ts` composable), so a fix that lives in a composable may not reach the browser until the dev server is killed and restarted — after editing a composable, restart `pnpm dev` to force the rebuild, verify the route returns HTTP 200 with an error-free log, and tell the user to hard-refresh (Cmd+Shift+R). Confidence: 0.6
- When sort/toggle state is mutated by a helper function invoked from the template (e.g. `@click="toggleSort(tenantSort, 'name')"` where the state object is passed as an argument), use `reactive({...})` rather than `ref({...})` for that state: Vue templates auto-unwrap top-level refs even when they are passed as function arguments, so a `ref` arrives in the function as its plain `.value` object and in-place mutations on the parameter (`sortState.key = ...`) never persist back to the ref — the sort header toggles its icon but the computed sorter never re-runs. `reactive` preserves object identity so the helper can mutate `key`/`dir` directly and the computed sorters react. (Debugged on the GRO dashboard's two-table sort, where `tenantSort`/`queueSort` were switched from `ref` to `reactive` after typecheck/render showed the ref-based toggle silently no-oped.) Confidence: 0.7
imeZone()).toISOString().split('T')[0]` (guarding invalid/empty values), and keep the form's ISO start/end strings in sync with a deep `watch` on the range plus a reverse `watch` on the target input that re-seeds the picker when the dialog opens with existing values. Confidence: 0.6
